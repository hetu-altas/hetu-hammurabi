# 20260809任务1 harness体系泛化 · 评审报告

> 节点：4 代码评审（charter-reviewer）｜评审日期：2026-08-09
> 评审对象：scripts/harness_topology.py、scripts/install_harness.sh、unit_test/test_harness_topology.py、unit_test/__init__.py、docs/harness/topology.md、.opencode/plugin/charter-gate.ts、8 个 agents、6 个 skills、8 篇文档（共 29 项文件）
> 门禁前置：单测 20/20 通过（unit_test/test/test_harness_topology_result.txt）；.gate.json 已写入任务目录

---

## 一、评审结论：**APPROVE**

本次改动实现完整、泛化彻底，全部验收标准达成。问题清单共 9 项，均为**低严重度**（既有缺口、观察项或文档小瑕疵），不阻断合入。核心实现（拓扑纯函数库 + 安装脚本 + 门禁插件）经实测验证正确。

---

## 二、问题清单

| # | 位置 | 严重度 | 问题 | 修改建议 |
|---|------|--------|------|---------|
| 1 | `docs/资源地图.md` L56 | 低 | 仍残留写死的具体项目路径引用：`hetu-thoth/scripts/weekly_pipeline.sh`、`docs/hetu-thoth/cron/weekly_pipeline_guide.md`（2026-08-08 任务1 历史资产登记）。实施计划未将其列入修改范围 | 判定为「历史登记/解释性文字」豁免范畴（验收标准 3 允许项），无需阻断；建议在任务1研发日志的 grep 回归结果中逐条归类说明，可选后续泛化为 `<业务项目>` 占位符 |
| 2 | 宿主仓库根 `.gitignore` 缺失 | 低 | 宿主无根 .gitignore（`git ls-files` 无 .gitignore、无全局 excludesfile），本次新增可执行脚本 `harness_topology.py` 运行产生 `scripts/__pycache__/`（git status 显示 `??` 未忽略），`git add -A` 时有误提交 pyc 风险；违反 project.md 3.1「.gitignore 必须含 `__pycache__/`」 | 宿主根 .gitignore 追加 `__pycache__/`、`*.py[cod]`；或本次提交时显式排除 `scripts/__pycache__/` 并清理。属既有缺口被本任务（首个可执行 Python 脚本）触发 |
| 3 | `docs/harness/assets.md` L13-25 | 低 | 顶层宪法目录树从「十一 Milvus 规范」直跳「十三 研发流程与宪章工作流」，缺少「十二 任务拆分评估」分支（L13 标题已正确修正为「13 章」，但树状图不完整） | 树状图补一行：`├── 十二 任务拆分评估` |
| 4 | `.opencode/agents/charter-notifier.md` L22 | 低 | 示例命令 `send_markdown('$TITLE', '''$BODY''')` 中 BODY 若含单引号/`'''` 会破坏 Python 字符串字面量（bash 拼接执行）；属提示词示例（与改造前行为一致），非代码缺陷 | 示例改用环境变量/临时脚本文件传参，或在示例处注明正文须转义单引号 |
| 5 | `scripts/harness_topology.py` `_cmd_dump` L277 | 低 | 宿主未找到时 `list_target_projects(workspace_dir, host_dir or workspace_dir)` 将 workspace 退化当作 host，targets 会包含真实宿主（调试输出误导） | 建议 detect 失败时直接显示「目标发现不可用（宿主未解析）」，或标注为调试近似结果 |
| 6 | `scripts/harness_topology.py` `detect_host_dir` L84 | 低 | 契约约定「无命中 → raise ValueError」，但 workspace 路径不存在/不可读时 `os.listdir` 抛 `FileNotFoundError`（异常类型与契约不一致） | 入口处校验目录存在并转换为 ValueError，或单测补充该输入场景 |
| 7 | `scripts/install_harness.sh` L17 | 低 | `PYTHON_BIN="${PYTHON_BIN:-python3}"` 默认使用系统 python3 而非共享 venv-hetu 解释器（topology.py 仅标准库，无实质影响） | 可默认 `$WORKSPACE_DIR/venv-hetu/bin/python`（含不存在回退 python3），非必须 |
| 8 | `unit_test/test_harness_topology.py` | 低 | 单测仅覆盖 6 个纯函数，未覆盖 CLI 入口（--dump/--json/--build-env/main） | 符合实施计划「单测只测纯函数本身」约定（CLI 已由 install 实机运行验证），列为观察项即可 |
| 9 | 宿主自身 `.opencode/.harness-env` | 低 | install 仅给目标项目生成 .harness-env，宿主自身无该文件；宿主内 gate.ts/agents 走回退路径（读宿主自身 docs/资源地图.md），功能等价正确 | 可选：install 也为宿主自身生成一份（需先解决「宿主 ≠ 目标」排除逻辑），非必须 |

---

## 三、逐项检查结论

### 1. 泛化彻底性（验收标准 2/3/5）— 通过
- `grep -rn "hetu-hammurabi\|hetu-aether\|hetu-mercury\|hetu-thoth\|venv-hetu"` 全文件域回归：**agents/skills 全部命中均为统一「回退规则」措辞模板**（8 agents + 6 skills 完全一致）；文档域命中全部为解释性文字/历史登记/字段契约（资源地图 L4 宿主说明、L56 历史资产登记、constitution 各章节架构描述、assets.md/README.md 部署语境、topology.md 背景与契约）；`scripts/install_harness.sh` **零命中**（脚本主体无任何具体项目名）；`charter-gate.ts` 零命中；`charter-logger.md` 无写死（全文核查通过）。
- `constitution/constitution.md` L32-33/L127/L139/L144 已泛化为「harness 宿主（运行期由 .harness-env 拓扑解析定位）」；L34 venv-hetu 保留并注明 VENV_BIN 解析。
- charter-analysis / charter-taskbook 技能已改为「先当前项目 src/docs → 再扫同父目录平级业务项目」匹配顺序（验收标准 5），并修正了原不存在的 `docs/hetu-mercury/tushare_interface/` 引用问题。

### 2. 一致性（.harness-env 六字段契约）— 通过
- 六字段（PROJECT_NAME / PROJECT_DIR / WORKSPACE_DIR / HARNESS_DIR / AETHER_DIR / VENV_BIN）在 `harness_topology.py`（模块 docstring + build_env_content）、`install_harness.sh`（对照表）、8 agents + 6 skills（统一措辞模板）、`topology.md`（字段契约章节）中措辞完全一致。
- 实测 4 个业务项目（sybil/mercury/thoth/aether）`.harness-env` 均已生成，六字段齐全且为绝对路径；软链 6 项（agents/commands/skills/plugin/package.json/package-lock.json）齐全；node_modules 保留实体（mercury/thoth 实测仍在，符合决策 2）；各项目 `.opencode/.gitignore` 均含 `.harness-env`（grep 计数=1，不重复追加），宿主 `.opencode/.gitignore` 同步追加。

### 3. 正确性 — 通过
- `harness_topology.py`：纯函数库（无文件写入、路径参数化），detect_host_dir 多候选按「要件数降序 → 目录名升序」确定性取优（实测 --json detect-host 正确返回宿主）；list_target_projects 排除宿主、extra 追加且不存在记入 skipped（实测 --json list-targets 返回 4 项目、排除宿主）；parse_env_content 空行/注释/引号剥离/重复键后者覆盖均正确；--dump/--json/--build-env 三个 CLI 入口实测运行正常。
- `install_harness.sh`：`set -euo pipefail`；宿主三要件校验（防拷贝误用）；`ln -sfn` 幂等覆盖；`.gitignore` 用 `grep -qxF || printf` 防重复追加（含文件尾换行补全）；输出对照表完整。
- `charter-gate.ts`：`resolveResourceMapPath` 先读 `.opencode/.harness-env` 的 HARNESS_DIR → 拼接宿主 `docs/资源地图.md`，缺失/空值/宿主地图不存在时回退当前项目（编排者决策 1 方案② 精确落地）；`tool.execute.after` 其余逻辑未动，无新增依赖（复用既有 existsSync/readFileSync/path）。

### 4. 安全 — 通过
- 无明文密钥/Token；无危险命令（install 仅软链 + 写 env + 追加 gitignore，无 rm 破坏操作）；无 SQL 拼接。
- gate.ts 插件块注释内无内嵌 `*/` 序列（仅 2 处正常块注释闭合符）；gates.md/extend.md 的 `*/` 警示保留。
- .harness-env 含绝对路径，已全部 .gitignore 忽略防入库。

### 5. 编码规范 — 通过
- `harness_topology.py`：首行 `# -*- coding: utf-8 -*-`、模块级 docstring、全量类型标注、Google 风格 docstring（Args/Returns/Raises）、命名小写+下划线、导入顺序（标准库→第三方→本地）合规、`if __name__ == "__main__"` 示例入口齐全、一行 ≤120 字符。
- `test_harness_topology.py`：unittest 框架、sys.path.insert 导入 scripts/、中文 docstring、tempfile 隔离 + tearDown 清理。
- `install_harness.sh`：注释中文、结构清晰、`set -euo pipefail` 完备。
- `unit_test/__init__.py`：空包标识合规。

### 6. 单测质量 — 通过（20/20）
- 三大类覆盖：正常 6 + 反例 7 + 边界 7（超出实施计划 16 用例清单，额外覆盖 no_agents_dir / empty_workspace / line_without_equal / missing_aether_venv）。
- 结果落盘 `unit_test/test/test_harness_topology_result.txt`：含测试总数/成功/失败/错误 + 失败/错误明细 + 「全部通过」结论，符合 unit_test.md「八、测试结果输出」run_tests 模式。
- .gate.json 已写入任务目录：`{"test_passed": true, "total": 20, "passed": 20, "updated_at": "2026-08-09 08:50:52"}`。

### 7. 文档质量 — 通过
- `docs/harness/topology.md`（新建）：背景/字段契约/回退规则/函数说明/安装流程/门禁联动/扩展点登记（venv 差异、多宿主、跨目录、node_modules）/FAQ 结构完整，已登记入资源地图「七、harness 体系文档」。
- README.md 技能数 ×5→×6 顺带修正；gates.md 2.4 与 gate 改造后行为一致（HARNESS_DIR 宿主资源地图 + 回退）；extend.md 安装分发更新 + 新项目接入三步 + FAQ Q4 改写；templates/task_book.md、task_split.md 均泛化。

### 8. 依赖与复用 — 通过
- requirements.txt 无变更（纯标准库实现，无新增依赖）。
- 无重复实现 hetu-aether `utils/` 能力（拓扑解析为全新机制，aether 无对应实现）；notifier 经 `$AETHER_DIR` + `$VENV_BIN` 调用既有 `utils/util_dingtalk.py`，行为不变。

---

## 四、合规自查结论

| 检查项 | 结论 |
|--------|------|
| 编码规范（文件头/类型/命名/导入/__main__） | 合规 |
| 逻辑正确性（边界/异常/空值） | 合规（2 项低严重度观察：_cmd_dump 退化、workspace 不存在异常类型） |
| 安全（凭据/SQL/危险操作/注释 */） | 合规 |
| 复用（hetu-aether utils） | 合规（无重复实现） |
| 依赖（requirements.txt） | 合规（无变更） |
| 测试覆盖（正常/反例/边界 + 落盘） | 合规（20/20，格式符合 unit_test.md） |
| 泛化彻底性（grep 回归） | 合规（命中均为解释性文字/回退规则/历史登记） |
| 六字段契约一致性（env/脚本/提示词/文档） | 合规 |
| 门禁文件（.gate.json） | 已生成（test_passed=true） |

**结论：REVIEW:APPROVE**
