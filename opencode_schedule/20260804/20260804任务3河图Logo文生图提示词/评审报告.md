# 评审报告：20260804任务3 河图Logo文生图提示词

> 评审节点：4（代码评审）｜评审者：charter-reviewer
> 评审日期：2026-08-08
> 评审对象：
> - `design/河图Logo文生图提示词.md`（193 行，6 章节主交付物）
> - `unit_test/test_prompt_doc.py`（509 行）与 `unit_test/test/test_prompt_doc_result.txt`（15 成功 / 0 失败）、`.gate.json`（test_passed=true, 15/15）
> - `实施计划.md`、`20260804任务3河图Logo文生图提示词.md`（任务书）
> 评审依据：`constitution/constitution.md`（顶层宪章）、任务书 A/B/C 三类验收、任务书第七节 15 条 A 类断言口径

---

## 一、评审结论

**REVISE（需修改）**

交付物本体（提示词文档）质量达标：B 类 6 条全部通过、A 类内容实测全部合规、宪章合规零风险。但 A 类**测试覆盖存在缺口**：任务书承诺"15 用例对应 A 类 15 条"，实际 A11（敏感词零命中）、A13（UTF-8 可读）两个断言函数已实现却未被任何测试用例调用（调用次数 0），与任务书硬门禁口径不符，需补用例后重测重审。

---

## 二、B 类设计质量逐条判定（软门禁）

| # | 检查项 | 判定 | 理由 |
|---|--------|------|------|
| B1 | 专业度与可投喂性 | **通过** | 8 段结构完整（主体→构图→风格→色彩→质感→光线→细节→质量词，段名【】标记清晰）；术语准确（墨分五色/飞白/焦墨/留白/朱砂点睛）；三态齐备可直接投喂：中文整段（通义万相/即梦）、英文 MJ 风格词（逗号分隔短语）、SD tag 词串（positive 框），参数按模型分别给基线，无需二次改写 |
| B2 | 河图体系定位契合 | **通过** | 卷轴母题（延续任务1 方案1 半展开横卷双轴杆/方案3 横幅）、朱砂点睛（任务1 方案3 朱砂上升箭头母题）、留白气韵（≥1/5 画布，对齐任务1 留白 ≥19% 规范）、K 线+曲折向上箭头量化语义（"规律在波动中被发现"）；色彩段引用二期低饱和色板语义（淡赭纸/焦墨/淡朱砂，≤3 色），**未混入一期高饱和色值**（已核对品牌文档 L241-247）；变体4 印章对齐任务2 10 号母题（36 见方、白文留白、无真实文字，与品牌文档 L268 语义一致） |
| B3 | 变体差异明显且可执行 | **通过** | 5 个变体互不相同（箭头形态/K 线疏密/留白比例/印章/画幅比例），非近义改写、非同图微调；每个变体含中英文可替换/追加片段，并标注替换位置（【细节】段/【构图】段/【主体】段），执行路径明确 |
| B4 | 负面提示词完备 | **通过** | 5 禁词（text/watermark/3d render/photorealistic/clutter）全含，扩展项 10 个（no logo/border/signature/frame/icon/shadow/gradient/barcode/grid lines/typography）；中英双语；给出三种挂载方式（MJ `--no`、SD negative 框、通义万相并入句尾"不要……"句式） |
| B5 | 英文版质量 | **通过** | 非机翻逐字：MJ 风格短语（"dry-brush flying white strokes"、"zigzag pullbacks"、"vermilion red accent dot"、"negative space"）符合文生图提示词习惯；8 段逐段对照表 + MJ 完整主词 + SD tag 三种形态分工明确 |
| B6 | 使用说明可落地 | **通过** | 4 模型适配表（投喂方式/参数写法差异/备注）全覆盖；5 类翻车现象→处置手段一一对应；3 步使用流程（首轮多模型对比→变体控制变量微调→精选重绘）清晰无含糊 |

## 三、A 类可断言项复核（15/15 已通过 + 独立实测复核）

评审者以只读脚本独立复核交付物（未修改任何文件）：

| 复核项 | 实测结果 |
|--------|----------|
| A1 交付物存在非空 | 通过（8498 字符） |
| A2 6 章节齐全 | 通过（前缀匹配全命中） |
| A3 6 锚点全命中 | 通过（横向+卷轴、K线、曲折+向上、红色+箭头、水墨、极简 全 AND/OR 命中） |
| A4 主提示词长度 | 通过（去空白 348 字，[100,500] 中段安全区） |
| A5 8 段结构 | 通过（8 段名全命中） |
| A6 负面 5 禁词 | 通过（小写化后全含） |
| A7 英文关键词 | 通过（scroll/arrow 必含，extras 全命中） |
| A8 参数宽高比+风格权重 | 通过（--ar 16:9、--stylize 100~250） |
| A9 变体 3~5 含片段 | 通过（变体1~5，各含替换/追加片段） |
| A10 使用说明双要素 | 通过（4 模型 + 乱码/拼贴/风格不统一） |
| A11 无敏感违规内容 | 内容通过（黑名单词零命中），**但无测试用例断言（见问题 1）** |
| A12 无 SVG/XML 产出 | 通过（design/ 递归 glob 仅 1 个 md） |
| A13 UTF-8 可读 | 内容通过（无 \ufffd），**但无直接用例断言（见问题 2）** |
| A14 反例（缺章节/长度越界） | 通过（2 个反例用例） |
| A15 边界（100/500 字） | 通过（含 99/501 越界对照） |

## 四、问题清单

| # | 位置 | 严重度 | 问题 | 修改建议 |
|---|------|--------|------|----------|
| 1 | `unit_test/test_prompt_doc.py` L215-218（`_check_sensitive`） | **中** | A11 断言函数已实现但**零调用**：没有任何 TestCase 断言"敏感词黑名单零命中"。任务书第七节承诺"15 用例对应 A 类 15 条"，A11 无对应用例，门禁对敏感内容回归无防护（当前文档内容实测合规，风险在未来修改） | 在 `TestPromptDocNormal` 新增 `test_no_sensitive_words`（断言 `_check_sensitive(self.full_text, SENSITIVE_BLACKLIST)` 为 True）；或并入 `test_doc_exists_not_empty`；若新增用例使总数变为 16+，需同步更新任务书/实施计划的用例计数口径 |
| 2 | `unit_test/test_prompt_doc.py` L228-235（`_utf8_readable`） | **中** | A13 断言函数已实现但**零调用**：`\ufffd` 替换字符检查未被任何用例断言。当前仅有间接防护（setUpClass 读取失败会使全部正常用例 error），但"UTF-8 可读且无乱码字节"这一 A13 承诺未直接兑现 | 在 `TestPromptDocNormal` 新增 `test_utf8_readable`（断言 `_utf8_readable(PROMPT_DOC)` 为 True），与问题 1 一并修复、一并重跑 |
| 3 | `unit_test/test_prompt_doc.py` L221-225（`_no_svg_files`） | 低 | 死函数：`_no_svg_files` 定义但未被调用（`test_no_svg_produced` 内联了相同 glob 逻辑，A12 实质断言有效）。属代码卫生问题 | 与问题 1/2 修复时一并处理：改为调用 `_no_svg_files(DESIGN_DIR)` 复用公共函数 |
| 4 | 计数口径文档 | 低 | 用例计数口径三方不一致：单测 docstring（正常 11/反 2/边界 2）vs 实施计划 §3.3（正常 9/反 2/边界 4）vs 任务书第七节（13 正常+1 反+1 反边）。三者均宣称对应 A 类 15 条，但 A11/A13 实际未落入任何用例 | 修复问题 1/2 后统一三处口径（建议：11 正常 = A1~A10+A12+A11+A13 调整，明确列出每条 A 类→用例名映射） |

## 五、文生图提示词工程专业改进建议（低危，不阻断，供编码节点择机采纳）

1. **措辞统一**：3.1 对照表第 5 行 "ink wash with **five shades of ink**" 与 3.2 完整主词 "ink wash with **five shades**" 不一致，建议统一保留 "of ink"（"墨分五色"信息完整性更高）。
2. **MJ 权重进阶**：当前仅 SD 给出 `(word:1.2)` 权重写法；建议在 5.2/6.1 补 MJ `::` 权重分段示例（如 `k-line chart::1.5, red arrow::1.8, scroll::1.2`），并注明 stylize 值越低越忠实于描述词，提升可控性。
3. **通义万相负面框**：6.1 适配表当前仅提供"负面约束并入句尾'不要……'句式"一种方式；通义万相新版若支持负面提示词输入框，可补充"直填 4.2 中文负面清单"选项。
4. **翻车规避 #5 因果强化**：6.2 表第 5 行"负面加 no watermark 防叠加水印干扰"与"箭头过细被吞"因果关系偏弱，建议改为"主词加 bold vermilion arrow / 强化朱砂色对比"或"采用变体3 加大留白比例提升箭头可见性"。
5. **细节段精简（可选）**：【细节】段"曲折向上的红色箭头蜿蜒折返后攀升"中"曲折向上"与"蜿蜒折返"语义重复，可精简为"一根朱砂红箭头蜿蜒折返、总体向上攀升"（不影响锚点命中，非必需）。

## 六、宪章合规自查

| 检查项 | 结论 |
|--------|------|
| 安全底线（无明文凭据/密钥/危险操作） | 通过：纯文档+纯标准库测试，无凭据、无数据库/网络操作 |
| 敏感内容（暴力/色情/政治敏感） | 通过：黑名单词零命中（评审者独立核对） |
| 纯文档无代码风险 | 通过：交付物为 markdown；单测纯标准库（unittest/re/os/glob/tempfile/shutil），零外部依赖 |
| 外部依赖/requirements | 通过：无新增依赖需求，无 requirements 变更 |
| 只读约束（任务1/2 不改、无 SVG/XML） | 通过：任务3 目录独立新增；design/ 仅 1 个 md；git 核查未发现对任务1/2 的改动（资源地图既有修改与任务3 无关，为历史未提交内容） |
| 单测规范（正常/反例/边界、tempfile 隔离、结果落盘） | 通过：反/边界全部 tempfile 临时目录 + tearDown 清理，未污染 design/；结果与 .gate.json 正确落盘 |

## 七、遗留事项

1. 修复问题 1/2/3（A11/A13 补断言、去死函数）后重跑单测（15/15 或按新计数 17/17），更新结果文件与 `.gate.json`，再提交复审；
2. 修复时同步统一问题 4 的计数口径表述；
3. 专业改进建议 1~5 由编码节点评估后择机采纳（不阻塞门禁）。

---

*评审结论：REVISE。交付物本体达标（B 类 6/6 通过、内容级 A 类全合规），问题集中于单测对 A11/A13 的断言覆盖缺失（函数已实现未挂用例），修复路径明确、成本低。*

---

# 复审结论（第 2 轮，charter-reviewer）

> 复审日期：2026-08-08｜复审对象：charter-coder 修复后的 `unit_test/test_prompt_doc.py`（529 行）、`design/河图Logo文生图提示词.md`（194 行）、`.gate.json`、`unit_test/test/test_prompt_doc_result.txt`
> 复审方式：只读核验（grep 调用计数 + 只读脚本实测 + `python3 -m unittest` 无落盘运行），未修改任何文件

## 八、复审结论

**APPROVE（通过）**

上轮 4 项问题全部修复、5 项专业建议全部落实；交付物本体 6 锚点/长度/6 章节复测通过；门禁 `.gate.json` 与结果文件一致（15/15）。修复无引入新问题，达到门禁放行标准。

## 九、上轮问题逐项核验结果

| # | 上轮问题 | 严重度 | 修复核验 | 结论 |
|---|----------|--------|----------|------|
| 1 | A11 `_check_sensitive` 零调用 | 中 | `test_prompt_doc.py` L336-340 新增 `test_no_sensitive_words`，断言 `_check_sensitive(self.full_text, SENSITIVE_BLACKLIST)` 为 True；grep 确认 `_check_sensitive` 现为 2 处（定义 L226 + 调用 L339），调用次数 1 > 0 | **已修复** |
| 2 | A13 `_utf8_readable` 零调用 | 中 | L348-351 新增 `test_utf8_readable`，断言 `_utf8_readable(PROMPT_DOC)` 为 True；grep 确认 `_utf8_readable` 出现 4 处（定义 L239 + 调用 L350 + docstring 提及 2 处），实际调用 1 > 0 | **已修复** |
| 3 | `_no_svg_files` 死函数 | 低 | `test_no_svg_produced`（L342-346）由内联 glob 改为调用 `_no_svg_files(DESIGN_DIR)`，死函数复活复用；grep 确认出现 2 处（定义 L232 + 调用 L345） | **已修复** |
| 4 | 计数口径三方不一致 | 低 | 三方已统一为 **15 = 正常 13 + 反例 1 + 边界 1**：单测 docstring L7-13（含 A 类→用例名映射 L17-27）＝实施计划 §3.3 L184（"正常 13 / 反例 1 / 边界 1，反例 14 含缺章节+缺锚点两类断言，边界 15 为四值四断言"）＝任务书第七节 L206（"正常案例 13 / 反案例 1 / 边界案例 1"）；13 个正常用例对应 A1~A13 逐条落位 | **已修复** |

## 十、专业建议落实核验（5/5）

| # | 上轮建议 | 落实位置 | 结论 |
|---|----------|----------|------|
| 1 | "five shades of ink" 统一 | 3.1 对照表 L65 与 3.2 完整主词 L73 均为 "ink wash with five shades of ink" | 已落实 |
| 2 | MJ `::` 权重分段示例 | 5.2 L122：`k-line chart::1.5, red arrow::1.8, scroll::1.2` + stylize 忠实度说明 | 已落实 |
| 3 | 通义万相负面直填 | 5.2 L125 与 6.1 适配表 L174 均补充"支持负面提示词输入框可直填 4.2 中文负面清单（与句尾句式二选一）" | 已落实 |
| 4 | 翻车表 #5 因果强化 | 6.2 L184：主词加 `bold vermilion arrow` 强化朱砂对比 + 或采用变体3 加大留白，因果链完整 | 已落实 |
| 5 | 【细节】段精简 | L47 已精简为"一根红色箭头蜿蜒折返、总体向上攀升"（去除"曲折向上"与"蜿蜒折返"语义重复）；6 锚点词 红色/箭头/蜿蜒/折返/向上/攀升 实测全保留 | 已落实 |

## 十一、主交付物最终状态确认（复审者独立只读实测）

| 复核项 | 实测结果 |
|--------|----------|
| 6 章节齐全 | 通过（主提示词/英文提示词/负面提示词/参数建议/变体/使用说明 前缀匹配全命中） |
| 6 锚点（9 检测项） | 通过（横向✓ 卷轴/画卷/横卷✓ K线✓ 曲折/蜿蜒/折返✓ 向上/上行/上扬/攀升✓ 红色✓ 箭头✓ 水墨✓ 极简✓，全命中） |
| 主提示词长度 | 去空白 **347 字**，∈ [100,500] 安全区（远离 100/500 边界） |
| 8 段结构 | 通过（主体/构图/风格/色彩/质感/光线/细节/质量词 全命中） |
| 【细节】段 6 锚点词 | 红色/箭头/蜿蜒/折返/向上/攀升 全命中 |
| UTF-8 可读 / 敏感词 | 无 `\ufffd`；黑名单 12 词零命中 |
| 门禁一致性 | `.gate.json`（test_passed=true, passed=15, failed=0, total=15, 2026-08-08T21:52:37）与 result.txt（总数 15/成功 15/失败 0/错误 0）数据一致，二者 mtime 同为 21:52:37（同一次运行落盘） |
| 测试实测 | 复审者以 `python3 -m unittest`（无落盘，只读）运行：**15/15 OK**，0.023s |

## 十二、宪章合规自查（复审补充）

| 检查项 | 结论 |
|--------|------|
| 编码规范 | 通过：文件头 `# -*- coding: utf-8 -*-`、模块级 docstring、函数类型标注齐全、导入顺序合规、无 `from x import *` |
| 新增用例质量 | 通过：`test_no_sensitive_words`/`test_utf8_readable` 均有 docstring 且断言与 A11/A13 口径一致；反/边界用例 tempfile 隔离 + tearDown 清理，未污染 design/ |
| 只读约束 | 通过：git status 确认改动仍限于任务3 目录（`opencode_schedule/20260804/` 整体未跟踪），无对任务1/2 的越界修改 |
| 外部依赖 | 通过：纯标准库，无 requirements 变更 |

**复审结论：APPROVE。4 项问题全部闭环、5 项建议全部落地，主交付物与门禁状态复测通过，准予放行进入后续节点。**
