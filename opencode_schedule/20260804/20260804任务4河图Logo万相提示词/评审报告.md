# 代码评审报告 · 20260804任务4 河图Logo万相提示词

> 评审节点：charter-reviewer｜评审日期：2026-08-09
> 评审对象：`design/河图Logo万相提示词.md`（275 行，6 章节）+ `unit_test/test_wanx_prompt.py`（15 用例）
> 评审依据：任务书 B 类软门禁 6 条（§B1~B6）+ A 类 15 用例复核；万相事实以任务书 §4.1 为权威基线
> 前置门禁：`.gate.json` test_passed=true（15/15，失败 0），结果文件 `unit_test/test/test_wanx_prompt_result.txt` 确认 15 成功

---

## 一、评审结论：**APPROVE**（通过）

B 类 6 条软门禁全部通过；A 类 15 用例单测已断言通过，且经评审独立复核脚本逐项验证关键事实（主提示词去空白 552 字 ∈ [200,800]；6 锚点全命中；ratio 求和 = 100；8 色 hex 全含；主提示词正文非万相参数词零命中；风险三要素齐备）。无高/中严重度问题，仅 4 条低严重度改进建议（均不阻断交付，供迭代参考）。

---

## 二、B 类逐条判定

| # | 判定项 | 结论 | 判定依据（实测/引证） |
|---|--------|------|----------------------|
| B1 | 万相特化准确性（无模型间混淆） | **通过** | 参数体系与任务书 §4.1 事实基线逐项一致：①模型 `wan2.7-image-pro` 主推（4K/thinking_mode/color_palette/n）、`wan2.6-t2i` 备选（同步调用+negative_prompt）；②size 为"宽\*高"星号格式，`2688*1536`/`4096*2304`/`2048*2048` 及 768\*768~4096\*4096、1:8~8:1 与事实 5 一致；③thinking_mode 默认 true、仅 wan2.7 系列（§57 行）；④prompt_extend 标注"wan2.7 系列不支持 → 显式 false 或不传"（§58 行），表述正确；⑤color_palette 3~10 色、ratio 总和 100%（§60 行）；⑥negative_prompt"wan2.7-image-pro / wan2.7-image 不支持、wan2.6-t2i 支持"（§103 行）表述准确；⑦n=2（主推）/1（备选同步）。**无模型混淆**：独立脚本复核主提示词正文非万相参数词（--ar/--stylize/CFG/steps 等）**零命中**；参数章节中 MJ/SD/CFG 词仅出现在 4 处明确标注"**通用口径，万相不适用**"的对照引用（§6/§95/§248/§271 行），符合任务书 B1 对照引用要求 |
| B2 | 中文长描述贴合万相风格 | **通过** | 主提示词为**自然连贯长段落**（7 分句、章节去空白 552 字 / 正文约 450 字），无【主体】式分段标题；宣纸纤维纹理、淡墨晕染渗化、墨分五色、飞白笔触、留白疏朗等官方水墨示例风格词逐句落实（§29 行），非标签堆砌、非机翻；§268 行撰写规范明确引用官方"毛笔水墨画风格，宣纸纹理清晰可见，淡墨晕染……"示例风格并说明 8 段语义骨架化为段落内分句 |
| B3 | 色板运用 | **通过** | 8 色 hex（`#F3E9D6`/`#3B3733`/`#57504A`/`#8D8578`/`#B7AFA2`/`#4E5D66`/`#A96B5F`/`#A9976B`）与 `docs/brand/河图体系Logo设计.md` 第六节二期低饱和色板逐一对账一致；ratio 分配 25/20/12/10/8/8/10/7（合计 100，实测求和验证），遵循"淡赭纸底 25 最大 → 焦墨主笔 20 次之 → 淡朱砂 10 点睛"河图用色逻辑，非随机均分，与任务书 §4.3 建议分配完全一致 |
| B4 | 可投喂性 | **通过** | API 双通道齐备：DashScope 原生（`input.prompt`，URL 完整）+ OpenAI 兼容（`messages.content.text`，URL 完整），两段 JSON 均含 model/parameters(size/thinking_mode/prompt_extend/color_palette 8 色数组/n)，字段名与官方一致、可直接调用；鉴权方式（Authorization Bearer + 控制台取 Key 提示）完整；百炼控制台 5 步操作含面板逐项参数对照（§236~242 行）；主提示词"整段复制直投"说明明确（§25 行） |
| B5 | 与任务3 传承一致 | **通过** | 6 组锚点正则与任务3 `test_prompt_doc.py::_check_anchors`（128~157 行）**逐行一致**（评审逐一比对确认）；负面要素清单 8 项承接任务3 5 禁词（text/watermark/3d render/photorealistic/clutter）+ 扩展项（logo/边框/签名/画框/图标/阴影/渐变/条形码/网格线/排版字样）并全部转为"不要出现"正向句式（§113~122 行）；卷轴/K 线/朱砂箭头/低饱和色板母题保留；变体语义对应任务3 变体3/4/5（留白 1/3、印章款、完全展开横幅）；负面句式计入正文字数口径与锚点断言无冲突（§134 行）；全文无自相矛盾 |
| B6 | 风险提示完备 | **通过** | 三要素齐备且各含规避动作：①DataInspectionFailed（§233/§256 行：自查敏感措辞 → n=1 + thinking_mode=false 降级重试 → 换 wan2.6-t2i）；②URL 24h 有效期下载归档（§232/§258 行，含控制台出图当天归档建议）；③版权合规（§234 行：生成图仅作底稿，定稿走人工精修与矢量重绘）。另覆盖 7 类翻车现象 → 万相侧规避手段（§250~258 行） |

**B 类综合判定：6/6 通过。**

---

## 三、A 类 15 用例复核摘要（单测已断言 + 评审独立复核）

| 类别 | 用例数 | 复核结果 |
|------|--------|----------|
| 正常案例 | 13（A1~A13） | `.gate.json` passed=15/failed=0；结果文件确认 15 成功；评审独立脚本复算：章节提取 6 前缀全命中、锚点 6/6 命中、主提示词去空白 552 字、ratio 数值真实解析求和=100（非仅匹配"100%"字样）、8 hex 全含、双通道字段、变体标记 3 个（A/B/C）、非万相参数词主文零命中 |
| 反案例 | 1（A14） | `test_reject_invalid_palette` 四类子断言：缺 1 hex 失败 / ratio 改 105 失败（含 sum≠100 前置自证）/ 9 组锚点词逐一删失失败 / 完整对照文档通过——测试自身有效性验证充分 |
| 边界案例 | 1（A15） | 恰 200/800 字通过、199/801 字失败，四值四断言，tempfile 隔离、tearDown 清理，不污染 design/ |
| 单测代码合规 | — | 文件头 `# -*- coding: utf-8 -*-`、模块级 docstring、函数参数/返回值类型标注齐全、导入顺序（标准库，组内有序）、纯标准库零依赖、结果写 `unit_test/test/` 并生成 `.gate.json`，符合 coding.md 与 unit_test.md |

---

## 四、问题清单

| # | 位置 | 严重度 | 问题 | 修改建议 |
|---|------|--------|------|----------|
| 1 | 文档头部 §7 行 | 低 | 事实基线仅引用"任务书 §4.1"，未直接给出官方文档 URL（任务书已核实：`help.aliyun.com/zh/model-studio/text-to-image`），用户自行核对官方更新时需回翻任务书 | 在 §7 行补注官方文档 URL（如"官方文档：阿里云百炼《千问qwen-image和万相wan模型文生图使用方式》help.aliyun.com/zh/model-studio/text-to-image，投喂前以官方最新文档为准"） |
| 2 | 变体C §157 行 | 低 | "印文以抽象块面示意"对万相可能被理解成普通色块而非印章意象，落地存在一定歧义概率 | 可补充质感细节增强印章辨识度，如"印面朱砂平涂、边缘留白、仿篆刻块面"（可选优化，不阻断） |
| 3 | 主提示词 §29 行 | 低 | 正文约 450 字，位于 [200,800] 中间偏下（实施计划目标 400~600 字内），语义密度尚有余量 | 如需更强细节可控性，可在质感/光线分句适度扩写至 500~600 字（可选，非必须；当前长度已在安全区） |
| 4 | color_palette §64~73 行 | 低 | 淡朱砂 ratio=10 高于清墨/黛青 8，与"点睛色占比直觉最小"略有张力 | 该分配与任务书 §4.3/§4.8 既定建议完全一致（10），语义上"点睛用色权重"成立，**建议维持现状**，仅作观察项不修改 |

> 说明：以上 4 条均为低严重度优化建议，不涉及事实错误、结构缺失或逻辑缺陷，不影响投喂可用性，结论维持 APPROVE。

---

## 五、合规自查结论

| 检查项 | 结论 | 说明 |
|--------|------|------|
| 编码规范（文档/单测） | 通过 | 单测文件头、docstring、类型标注、命名、导入顺序合规；文档结构清晰、章节齐全 |
| 安全底线 | 通过 | 无凭据硬编码（API Key 以 `<DASHSCOPE_API_KEY>` 占位符形式说明）、无 SQL/网络危险操作（纯文档任务） |
| 复用 | 通过 | 6 锚点正则、`_extract_section`/`_check_sensitive`/`_no_svg_files`/`_utf8_readable`/tempfile 反边界写法均复用任务3 口径并标注；无重复造轮子 |
| 依赖 | 通过 | 纯标准库（unittest/re/os/glob/tempfile/shutil），零新增依赖，无需变更 requirements.txt |
| 测试覆盖 | 通过 | 正常 13 + 反例 1 + 边界 1 = 15/15，`.gate.json` test_passed=true；反例验证测试自身有效性，边界四值四断言 |
| 流程合规 | 通过 | 评审只读（未修改任何源码/交付物）；任务1/2/3 文件只读未改（git 状态无越界修改）；design/ 下无 .svg/.xml（实测 0 个）；未实际调用万相 API |
| 传承一致性 | 通过 | 与任务3 文档/单测锚点口径逐行一致，母题与负面语义承接无矛盾 |

---

*评审结论：**APPROVE**——可进入节点5 研发日志与后续流程（资产沉淀须按任务书第九节追加品牌文档第八节与资源地图第八节登记）。*
