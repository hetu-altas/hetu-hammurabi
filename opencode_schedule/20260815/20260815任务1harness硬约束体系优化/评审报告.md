# 评审报告 · 20260815任务1 harness硬约束体系优化

> 节点：charter-reviewer（代码评审）
> 评审对象：H1-H9 九项优化（规则外置/绕过面收窄/seal 核对/续签/脱敏/资产检查/密钥/并发）
> 单测证据：215/215 全绿（test_harness_dsh_result.txt 195 + test_harness_topology_result.txt 20），
> .gate.json v2 已落闸且 gate_token 校验通过（评审实测验证）
> 评审方式：只读通读全部 23 项改动文件 + 实测复算（token 校验 / 规则加载异常路径）

---

## 一、评审结论

**REVISE（需修改）**

总体质量良好：契约不变红线全部守住（build_gate_v2/gate_open 判定链/R1-R3/is_*/decide 签名零改动，
8 个既有测试文件零回归）；规则外置、绕过面正则、seal 双格式、re-seal、并发锁、脱敏/资产迁移的实现与
实施计划基本一致，单测覆盖正常/反例/边界完整。但存在 **2 处代码缺陷 + 1 处环境未落地**，需修复后重测重审。

---

## 二、问题清单

| # | 位置 | 严重度 | 问题 | 修改建议 |
|---|------|--------|------|---------|
| 1 | `harness/core/gate.py` L164-184（validate_rules） | **高** | **fail-closed 违约（崩溃而非回退）**：`_compile_rules(rules)`（L172）在 backup 段校验（L175-177）**之前**执行，而 `_compile_rules` 直接访问 `rules["backup"]["pattern"]`。实测：规则文件缺 `backup` 段或 `backup.pattern` 时抛 `KeyError: 'backup'` / `KeyError: 'pattern'`，且 `validate_rules` 只捕获 `re.error`、`load_rules` 只捕获 `yaml.YAMLError`，异常直接逃逸到判定调用链（decide/is_destructive 崩溃）。违反验收 1「非法规则 fail-closed」与实施计划 3.1「schema 非法→回退内置默认并告警」。 | 将 backup 校验（含 pattern 类型/存在性）移到 `_compile_rules` 调用之前；或在 `validate_rules` 中对 `_compile_rules` 统一捕获 `(re.error, KeyError, TypeError)` 返回 `(False, msg)`。补充单测：缺 backup 段 / 缺 backup.pattern 两种 yaml 均回退默认且不抛异常。 |
| 2 | `harness/core/cli.py` L209（cmd_redact）+ `harness/gate_rules.yaml` L79-82 | 中 | **secret_patterns 段未接线（声明与实现不符）**：`cmd_redact` 直接调用 `redact.redact_secrets(args.text)`，全程无任何代码读取 `gate_rules.yaml` 的 `secret_patterns` 并传入；`redact.compile_patterns` 也无人调用（仅单测用）。用户修改 yaml 的 secret_patterns 不生效（死配置），与 gates.md「模式可在 gate_rules.yaml secret_patterns 段配置」及实施计划 3.3.1「若含可选 secret_patterns 段则优先」不符。默认脱敏行为本身正确（内置三类与旧版一致）。 | `cmd_redact`（或 redact_secrets 默认路径）从 `gate.get_effective_rules()["secret_patterns"]` 读取并 `compile_patterns` 后传入；注意第 3 类键名大小写不敏感需在编译时补 `re.IGNORECASE`（内置默认有、自定义无，行为不一致）。补单测：修改 yaml secret_patterns 后 cli redact 生效。 |
| 3 | `conf/gate_secret`（权限）+ `scripts/install_dsh.sh` L66 | 中 | **验收 7 环境未落地**：实测 `conf/gate_secret` 权限仍为 **777**（install 脚本的 `chmod 600` 修正未实际执行）。代码与单测正确（test_gate_concurrency 权限用例全绿），但运行环境状态未达标。 | 执行 `bash scripts/install_dsh.sh`（或至少 `chmod 600 conf/gate_secret`）并复核 `stat -c '%a'`，在研发日志登记权限修正结果。 |
| 4 | `docs/hetu-hammurabi/hard-gate-optimize.md` L20 | 低 | 文档数据滞后：写「改造后 194/194（新增 84 用例）」，实际回归 215/215（dsh 195 + topology 20，新增 105）。 | 更新为 215/215，并注明两 result 文件口径。 |
| 5 | `unit_test/test_gate_rules.py` L134-146 | 低 | 测试名/注释声称验证「mtime 变更重载」，实际用 `reset_rules_cache()` 绕过 mtime 检测（注释「显式 touch 纳秒」未实现）。mtime 分支（`stat().st_mtime_ns` 比对）无直接用例覆盖。 | 补一个用例：写入文件后 `os.utime` 改 mtime，不调 reset 直接验证 `get_effective_rules()` 返回新值（真实验证 mtime 重载路径）。 |
| 6 | `harness/plugins/charter-gate.ts` / `charter-orchestrator.ts` resolveEnv | 低 | 宿主自身场景（.harness-env 缺失）VENV_BIN 为空 → 回退 `python3`（非 venv-hetu，可能缺 pyyaml）。属 20260814 既有模式，非本次引入，仅提示。 | 后续可在 resolveEnv 增加：宿主自身时探测 `../venv-hetu/bin/python`，本次可不改。 |

---

## 三、逐项核对（评审范围全部通过项）

### 1. 契约不变红线 ✅ 零改动
- `build_gate_v2` / `compute_gate_token` / `verify_gate_token` / `parse_gate_timestamp` 签名与逻辑不变；
- `gate_open` 判定链 MISSING→SCHEMA→RUN_ID_MISMATCH→NOT_PASSED→NO_RESULTS→TOKEN_INVALID→STALE 顺序、R1-R3 语义、返回码常量全部保留；`max_age_seconds` 参数保留（decide 内取 rules 值，显式传入可覆盖）；
- `is_log_file_write` / `is_notify_call` / `is_allowed_notify` / `is_destructive` / `has_backup_declaration` / `decide` 签名不变；
- 既有 8 个测试文件（test_gate_token 等）回归全绿，test_gate_token 14 用例守护 build_gate_v2 契约；
- 历史 .gate.json 兼容：本次 20260815 落闸经评审实测 `verify_gate_token=True`、`gate_open` 可开闸。

### 2. 规则外置正确性 ✅（缺陷 #1 除外）
- `gate_rules.yaml` 与内置默认逐字段一致（yaml 双引号转义已实测核对：`\\\\rm`→`\\rm`、`\\b`→`\b` 与 raw 字符串字面一致）；
- mtime 重载、非法 yaml / 根节点非 mapping / 类型错 / 非法正则均回退默认并告警（测试覆盖 7 个反例）；
- 正则正确性（实测 + 31 用例）：rm 变体 5 种（-rf/-fr/-r -f/--recursive --force//bin/rm/\\rm）全拦；shred/unlink/rmdir -r/mv trash 全拦；`oapi[.]dingtalk[.]com` 字符类与 `"oapi."+"dingtalk.com"` 拼接宽松式均命中；
- 备份语义：销毁判定用原始 cmd（`echo "rm -rf"` 仍拦），备份搜索用剥离 echo/printf/注释后文本；假备份 4 样本拦截、真备份 4 样本放行（test_gate_bypass L108-133 已按实施计划同步调整，用例数不变）。

### 3. seal 核对 ✅
- 八格式四数字齐全才算命中；原生格式 OK/FAILED 判定仅限 `Ran N tests` 之后汇总段（H3 回归修复正确，test_dual_format_body_contains_failed_word 守护）；
- 双格式交叉核对不一致拒绝；任一文件失败/无法解析 → 整体拒绝（SealError），且不落闸（test_seal_failed_result_rejected_no_gate_written 断言 .gate.json 不存在）；
- 落闸数字与 result 一致：.gate.json total=215 = dsh 195 + topology 20，token 校验通过；
- CLI 已删 `--total/--passed`；orchestrator/command.ts 同步（re-seal 用法提示已补）。

### 4. re-seal / 密钥 / 并发 ✅
- re_seal 只刷 updated_at + 重签 token，run_id/result_files/total/passed/test_passed/written_by 原样（10 用例守护）；重读 result 重新核对（被改/缺失拒绝）；schema/run_id 不符拒绝；
- `recorder.VALID_EVENT_TYPES` 已追加 `re_seal`（纯追加，test_recorder 零改动回归）；审计事件落盘字段（status/project/detail.refreshed/total）验证通过；
- rotate_secret：临时文件 + fsync + rename 原子写 + chmod 600 + 无 .tmp 残留；force 语义正确；旧密钥 token 失效验证通过；
- write_gate_locked：flock 独占锁 + 锁内重读 + 损坏文件拒绝 + updated_at 比对拒绝「已被并发更新」；双进程实测串行化（test_parallel_seal_serialized）。

### 5. 脱敏 / 资产检查 ✅
- redact.py 三类默认模式与旧版 `.opencode/plugin/charter-gate.ts` L80-84 逐字一致（含 IGNORECASE），subn 计数与旧 replace 累计语义一致；正常文本零误伤（URL/中文/短值 <8 位）8 用例全绿；
- assets_check.py 迁移旧版 L56-78/L148-158：三级回退（.harness-env 缺失 / HARNESS_DIR 字段缺失 / 宿主地图不存在 → 当前项目地图）实测正确；basename includes 语义一致；非 docs/hetu-*/ 跳过。

### 6. 安全 ✅
- 密钥内容未泄漏：gate_secret 前 8 位扫描代码/脚本/文档/测试零命中；test 中 SECRET 均为测试常量；
- 无危险命令拼接（rotate 用参数列表调 openssl，无 shell 注入）；install_dsh.sh heredoc 生成规则文件；
- TS 注释无 `*/` 序列（gates.md 已含对应提醒）；charter-gate.ts 拦截逻辑只经 Python 核心，插件为薄适配。

### 7. 编码规范 ✅
- 全部 Python 文件首行 `# -*- coding: utf-8 -*-` + 模块级 docstring（含 H1-H9 修复对照）；
- 函数参数/返回值类型标注完整；导入顺序标准库→第三方（yaml）→本地；命名小写+下划线；无 `from module import *`；
- 新增依赖：pyyaml 共享 venv 已有（6.0.2 实测），无 requirements.txt 变更需求（宿主无独立 requirements，属既有结构）。

### 8. 单测质量 ✅（缺陷 #5 除外）
- 7 个新增/修改测试文件覆盖正常/反例/边界，结果落盘 unit_test/test/ 且为最新（Ran 195 + 20 = 215 OK）；
- Mock 隔离：gate_lease/concurrency 均用 tempfile + 子进程隔离外部依赖，无真实 runlog/密钥污染；
- .gate.json 落闸：test_passed=true、token 校验通过（评审实测复核）。

---

## 四、合规自查结论

| 检查项 | 结论 |
|--------|------|
| 契约不变红线（build_gate_v2/判定链/R1-R3/函数签名） | ✅ 零改动，回归守护 |
| 不动边界（constitution/.opencode 旧插件/20260814 产物/存量 .gate.json/runlog 历史/notify.py） | ✅ 未触碰（.opencode/plugin/charter-gate.ts 仅作只读迁移源） |
| 数据安全（无明文密钥、无危险操作） | ✅ 密钥零泄漏；rotations 原子写 |
| AI 分析范式合规（本任务无分析判断逻辑，纯机制代码） | ✅ 不适用 |
| 任务书 11 项验收 | ✅ 10 项通过；验收 1 部分违约（缺陷 #1 崩溃路径）、验收 7 环境未落地（缺陷 #3） |

**结论：REVISE** —— 修复问题 1（validate_rules 崩溃路径，必修）、问题 2（secret_patterns 接线）、问题 3（密钥权限落地）后，重跑全量单测（含新增反例）并重新落闸，再提交复审。

## 五、评审元信息

- 评审人：charter-reviewer（deepseek-v4-flash）
- 评审时间：2026-08-15
- 复算验证项：.gate.json token（True）；backup 缺失异常路径（KeyError 实测复现）；密钥前 8 位泄漏扫描（零命中）；conf/gate_secret 权限（777 实测）

---

# REVISE 第1轮修复记录（2026-08-15 · charter-coder）

> 依据本报告「二、问题清单」逐项修复，原评审内容未改动，本表仅追加记录。

| # | 原问题 | 严重度 | 修复状态 | 修复说明（文件 + 行为） |
|---|--------|--------|---------|------------------------|
| 1 | validate_rules 中 `_compile_rules` 先于 backup 校验执行，缺 backup 段/backup.pattern 时 KeyError 逃逸导致 decide 崩溃，违反 fail-closed | 高 | ✅ 已修复 | `harness/core/gate.py`：backup 段校验（含 pattern 存在性与非空）前移至 `_compile_rules` 调用之前；编译调用统一捕获 `(re.error, KeyError, TypeError)` 返回 `(False, msg)` 回退内置默认。端到端复测：缺 backup 段/缺 backup.pattern 的规则文件下 `decide` 不崩溃、回退默认并拦截（DATA_SAFETY_BLOCKED）。新增反例单测：`test_gate_rules.py` `test_backup_section_missing_rejected` / `test_backup_pattern_missing_rejected` |
| 2 | gate_rules.yaml 的 secret_patterns 段无代码读取（死配置），用户修改不生效；自定义键值对模式缺 IGNORECASE | 中 | ✅ 已修复 | `harness/core/cli.py` cmd_redact 从 `gate.get_effective_rules()["secret_patterns"]` 读取并 `compile_patterns` 传入 redact_secrets（缺省回退内置默认）；`harness/core/redact.py` `compile_patterns` 对含键名词汇（password/passwd/pwd/secret/token/api[_-]?key/access_key 等，IGNORECASE 检测）的模式自动补 `re.IGNORECASE`，与内置第 3 类语义一致。端到端复测：yaml 自定义 secret_patterns（含 `CLIENT_SECRET=` 大写键名）经 `cli redact` 命中并脱敏。新增单测：`test_gate_redact.py` `TestRulesSecretPatterns` 3 例（生效/IGNORECASE/缺省回退） |
| 3 | conf/gate_secret 实测仍 777，验收 7 环境未落地 | 中 | ✅ 已落地（含环境限制登记） | 已执行 `chmod 600 conf/gate_secret` 与复核：**WSL /mnt/d 为 9p/drvfs 挂载且无 metadata 选项，chmod 权限位不生效（实测仍 777）**——`scripts/install_dsh.sh` L66 `chmod 600` 逻辑正确（权限断言单测在 /tmp ext4 验证通过），原生 Linux 部署环境 install 后即为 600；环境限制已登记 `docs/hetu-hammurabi/hard-gate-optimize.md` 已知限制第 5 条（不伪造成功） |
| 4 | hard-gate-optimize.md L20 测试数 194/194 滞后 | 低 | ✅ 已更新 | 更新为最新口径：`discover` 全量 201/201 + 存量 topology 20 = 落闸口径 **221/221**，注明两 result 文件口径 |
| 5 | test_gate_rules.py L134-146 声称 mtime 重载实为 reset_rules_cache 绕过，mtime 分支无直接用例 | 低 | ✅ 已补 | 新增 `test_real_mtime_reload_without_reset`：改文件内容 + `os.utime` 刷新 mtime 后**不调 reset_rules_cache**，直接验证 `get_effective_rules()` 返回新值（真实 mtime 重载路径） |
| 6 | charter-gate.ts/orchestrator.ts resolveEnv 宿主自身场景 VENV_BIN 回退 python3 可能缺 pyyaml | 低 | ✅ 已登记 | 属 20260814 既有模式，本次不改代码；已登记 `docs/hetu-hammurabi/hard-gate-optimize.md` 已知限制第 6 条（后续可在 resolveEnv 探测 `../venv-hetu/bin/python`） |

## 重测与落闸（修复后）

- 全量重测：`../venv-hetu/bin/python -m unittest discover -s unit_test -p "test_*.py"` → **201/201 全绿**（原 195 + 新增 6），结果已更新 `unit_test/test/test_harness_dsh_result.txt`
- 重新落闸（宿主根执行）：seal-gate --results dsh(201) + topology(20) → **.gate.json total=passed=221，test_passed=true，token 校验通过，gate_open GATE_PASS**
- 修复文件：`harness/core/gate.py`、`harness/core/cli.py`、`harness/core/redact.py`、`unit_test/test_gate_rules.py`、`unit_test/test_gate_redact.py`、`unit_test/test/test_harness_dsh_result.txt`、`docs/hetu-hammurabi/hard-gate-optimize.md`（+本报告追加记录）

---

# 复审结论（第 1 轮 REVISE 复审 · charter-reviewer）

> 依据编码节点追加的「REVISE 第1轮修复记录」逐项复核（原评审内容零改动），全部基于真实代码实测。

## 最终结论：**REVIEW:APPROVE**

三项必修复项全部闭环，低严重度项已更新/登记；重测 201/201 全绿，落闸 221/221 且 token 校验、gate_open 经评审实测通过。无未闭环项，无需第 2 轮。

## 逐项复核结果

| # | 原问题 | 复核方式 | 结果 |
|---|--------|---------|------|
| 1（高） | validate_rules fail-closed 违约（KeyError 逃逸） | 通读 gate.py L170-181 + 实测复算 | ✅ **闭环**：backup 校验（dict + pattern 非空）已前移至 `_compile_rules` 之前（L172-175）；编译调用统一捕获 `(re.error, KeyError, TypeError)` 返回 `(False, msg)`（L177-181）。实测：缺 backup 段 / 缺 backup.pattern 的规则文件均回退内置默认（freshness=600）并告警「backup.pattern 缺失或非字符串」，`decide` 端到端不崩溃（返回 DATA_SAFETY_BLOCKED）。新增反例 `test_backup_section_missing_rejected` / `test_backup_pattern_missing_rejected` 断言告警文本 + 回退值 + 判定链路可用，质量合格 |
| 2（中） | secret_patterns 死配置 + 自定义键值对缺 IGNORECASE | 通读 cli.py cmd_redact + redact.py compile_patterns + 实测 | ✅ **闭环**：`cmd_redact` 经 `gate.get_effective_rules().get("secret_patterns")` 读取，非空则 `compile_patterns` 传入，缺省回退内置默认；`compile_patterns` 以 `_KV_PATTERN_HINT`（键名词汇 IGNORECASE 检测）对键值对类模式自动补 `re.IGNORECASE`，与内置第 3 类语义一致。实测默认文件 3 个模式正常编译。新增 `TestRulesSecretPatterns` 3 用例（生效覆盖/大写键名 IGNORECASE/缺省回退），走与 cli 一致的读取路径，质量合格 |
| 3（中） | conf/gate_secret 777 环境未落地 | 实测权限 + 通读登记说明 | ✅ **闭环（环境限制真实登记，不伪造成功）**：实测权限仍 777，但 `hard-gate-optimize.md` 已知限制第 5 条已真实登记——WSL `/mnt/d` 为 9p/drvfs 挂载且无 metadata 选项，chmod 权限位不生效；install 脚本 `chmod 600` 逻辑正确、权限断言单测在 /tmp（ext4）验证通过、原生 Linux 部署 install 后即为 600。说明真实可核验，符合验收 7 的本环境交付口径 |
| 4（低） | hard-gate-optimize.md 194/194 滞后 | 通读修复记录 + 实测 | ✅ 已更新为 201/201 + topology 20 = 221/221 口径 |
| 5（低） | mtime 重载无直接用例 | 通读 test_gate_rules.py L188-199 | ✅ 已补 `test_real_mtime_reload_without_reset`：改文件内容 + `os.utime` 刷新 mtime 后**不调 reset_rules_cache**，直接断言 `get_effective_rules()` 返回新值 1200——真实覆盖 mtime 重载分支 |
| 6（低） | resolveEnv 宿主自身 VENV_BIN 回退 | 通读登记说明 | ✅ 已登记已知限制第 6 条（20260814 既有模式，后续探测 `../venv-hetu/bin/python`），本次不改代码合理 |

## 单测与落闸复核（评审实测）

- `unit_test/test/test_harness_dsh_result.txt`：**Ran 201 tests · OK**（原 195 + 新增 6，含 2 反例 + 1 mtime + 3 secret_patterns）
- 任务目录 `.gate.json`：total=passed=**221**（201 + topology 20）、test_passed=true、schema_version=2、run_id 一致、**token 校验 True**、**gate_open → (True, GATE_PASS)**（全部评审实测）
- 契约不变红线复核：gate_open 判定链 / build_gate_v2 / is_* / decide 签名零改动，既有 8 测试文件回归全绿

## 遗留观察项（低，登记不阻断）

1. 当前 WSL 环境 `conf/gate_secret` 权限仍为 777（drvfs 限制，已登记）；建议原生 Linux 部署后实际执行一次 `bash scripts/install_dsh.sh` 并 `stat -c '%a'` 复核 600 落地。
2. `test_gate_rules.py` `test_change_main_pattern_takes_effect` 注释仍写「显式 touch 纳秒」但实现用 `reset_rules_cache()`（真 mtime 路径已由新用例覆盖，注释瑕疵不影响正确性，可随下次任务顺手修正）。
3. `TestRulesSecretPatterns` 用例模拟 cli 读取路径而非直接调用 `cmd_redact`（cli 为薄壳，模拟路径与实现逐行一致，可接受）。

---

**复审元信息**：charter-reviewer（deepseek-v4-flash）· 2026-08-15 · 实测项：缺 backup 异常路径（回退+告警+不崩溃）、secret_patterns 编译、token 校验 True、gate_open GATE_PASS、dsh result 201 OK、密钥权限 777（drvfs 登记属实）
