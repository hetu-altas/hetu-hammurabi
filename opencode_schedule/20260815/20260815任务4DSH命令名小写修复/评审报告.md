# 代码评审报告 · 20260815任务4 DSH命令名小写修复

> 节点：charter-reviewer（代码评审）｜评审时间：2026-08-15
> 任务书：`20260815任务4DSH命令名小写修复.md`｜实施计划：`实施计划.md`
> 评审对象：charter-command.ts（8 处小写化）、command.test.mjs（3 处同步）、client.js（L234 注释）、dsh-migration.md（L73/L75）、资源地图.md（L84 注记）、test_cc_command.py（+2 用例）、web profile 部署产物
> 评审方式：只读评审（未修改任何源码）；宪章依据：constitution.md / coding.md / project.md（宿主即 hetu-hammurabi，`.harness-env` 缺失按回退规则①动态确认）

---

## 一、评审结论

**APPROVE（通过）**

本次改动修复彻底、测试有效、docs 与部署同步、不动边界零越界、回归无退化，符合编码宪章与任务书验收标准 1-6。发现的 2 项均为低严重度流程文档/口径表述问题，不影响交付质量，不构成 REVISE 条件。

## 二、问题清单

| # | 位置 | 严重度 | 问题 | 修改建议 |
|---|------|--------|------|----------|
| 1 | `研发流程状态.md` L11-17 | 低 | 「编排决策」表格插在节点记录表中间（位于两条「1 分析」记录行之间），表结构不连贯 | 后续编排器追加记录统一追加至表格末尾，决策区独立成节，避免插入记录表中间 |
| 2 | 实施计划 §4.3 / 任务书 §7.3 落闸构成估算 | 低 | 估算「220 + 扩展后 cc 用例 10 + 拓扑 20 = 250」，实际落闸 242（= 222 + 20）——因 220 基线本身已含 cc 8 旧用例，扩展后为 222（212 + 10），估算口径把含 cc 的 220 与 cc 用例数重复相加 | 后续任务落闸构成表述统一口径（如「基线 212 + cc 10 + 拓扑 20 = 242」）；本次 .gate.json 实际 242/242 数字核对正确，无实质影响 |

> 备注（不构成问题）：`.gate.json` 的 `result_files` 未单列 `test_cc_command_result.txt`，但其 10 用例已全部包含于 `test_harness_dsh_result.txt`（222 中，grep 实证 10 处），242 = 222 + 20 与落闸总数自洽，门禁有效性不受影响。

## 三、逐项检查记录

### 3.1 修复彻底性 —— 通过

charter-command.ts 8 处小写化逐行核实（与实施计划 §3.1 完全一致）：

| 行号 | 现状 | 核实 |
|---|---|---|
| L2 | `* charter-command · 宪章编程 /cc 命令（DSH web GUI 钩子）` | ✓ |
| L4 | `` 用户在 GUI 输入框输入 `/cc <任务书路径 或 一句话需求>` `` | ✓ |
| L14 | `* 命令名：cc（constitution coding，宪章编程）。`（清单外残留已修复） | ✓ |
| L24 | `const name = "cc";` | ✓ |
| L75 | `用户通过 /cc 命令启动了宪章研发流程。` | ✓ |
| L133 | `name: "cc",` | ✓ |
| L150 | `请提供任务书路径或一句话需求：/cc <任务书路径 或 一句话需求>（...）` | ✓ |
| L166 | `[charter-command] /cc 命令已注册（...）` | ✓ |

- 兜底扫描：`grep -c "CC" charter-command.ts` = **0**；`harness/dsh/` 全域 = **0**（charter-command.ts / client.js / command.test.mjs 三文件全部清零）
- 命令名合规：`cc` 匹配 DSH 校验正则 `/^[a-z][a-z0-9_-]*$/u`（实证 `node_modules/@deepseek-ai/dsh-commands/lib/index.js:68` `const COMMAND_NAME = /^[a-z][a-z0-9_-]*$/u;`，L75 抛错点与用户报错一致）；`"cc"` 全小写字母必然通过
- 导出形态零改动：L169 `export { name, inject, apply };`、L170 `export default { name, inject, apply };` 原样保留；`inject = ["commands"]`（L25）与 handler 逻辑（findHarnessDir / parseInput / buildCharterPrompt / apply）全文通读，除 8 处命令名文本替换外零改动
- 块注释完整性：L1-17 头注释结构完整，替换文本不含 `*`/`*/` 序列，无 `*/` 逃逸风险

### 3.2 测试有效性 —— 通过

- **command.test.mjs**（96 行）：L38 标题「插件注册 /cc 命令」、L42 `assert.equal(cmd.name, "cc")`、L79 `assert.match(result.text, /\/cc/)` 三处同步到位；**评审独立重跑 6/6 全过**（fnm v24.19.0 全路径，`--experimental-strip-types` 实测可用；默认 PATH node v18.19.1 不支持 strip-types 的环境事实已登记）
- **test_cc_command.py 新增 2 用例**（8 旧 + 2 新 = 10/10，result 落盘实证）：
  - `TestDshPluginCommandNameLowercase`（正常案例）：断言 `const name = "cc";` 与 `name: "cc",` 存在；正则提取命令名常量后 `re.fullmatch(r"[a-z][a-z0-9_-]*", name)` 校验（Python 侧等价 DSH 正则）；反案例断言 `name = "CC"` / `name: "CC"` 不存在 —— 真实断言，非空转
  - `TestNoCcRefInDshAndDocsDomain`（反案例）：对 charter-command.ts / client.js / command.test.mjs / dsh-migration.md 4 文件扫描 `/CC`、`"CC"`、`命令名：CC` 零命中；白名单为空集——**合理**（dsh-migration.md 全文已核实无修订记录区含大写表述，L73/L75 为正文引用必须改）
  - 既有 8 用例与 `DEV_REF_PATTERNS` / `REVISION_WHITELIST` / `DOCS_DOMAIN_FILES` / 辅助函数（`read_text` / `scan_dev_ref`）零改动，无回归风险

### 3.3 docs 一致性 —— 通过

- `dsh-migration.md`：L73 `GUI 钩子（/cc 命令，constitution coding；...）`、L75 `#     /cc <任务书路径 或 一句话需求>` 已改小写；全文 grep `/CC` 零命中
- `资源地图.md` L84：「L73 大写 /CC 保留未误动」表述已删除，更新为「20260815 任务4 更新：命令名统一小写 cc（DSH 命令名校验要求小写开头）」—— 与实施计划 §3.4 完全一致

### 3.4 部署一致性 —— 通过

- md5 双侧全同：`7baab9d9845a143071ce8883ebd73c85`（宿主 `harness/dsh/plugins/charter-command.ts` == `/home/qmt/.dsh/profiles/web/plugins/charter-command.ts`），`diff` 逐字节内容一致（实体拷贝部署）
- 注册配置核实：web profile `cordis.patch.yml` 含 `id: charter-command` + `name: /home/qmt/.dsh/profiles/web/plugins/charter-command.ts` + `config.harnessDir: /mnt/d/workspace/hetu-altas/hetu-hammurabi`
- 启动验证：流程状态记录「dsh web 完整启动成功（node bin.js 方式，npm bin 权限问题已登记）」—— 达到任务书验收 5 首选方案；即使降级口径下，命令名校验必然通过（正则实证 + `name="cc"` 确定性事实）

### 3.5 不动边界 —— 通过

| 文件 | mtime | 判定 |
|---|---|---|
| `.opencode/commands/cc.md` | 08-15 13:43（任务3 窗口） | ✓ 零改动 |
| `constitution/**`（constitution.md 13:44；coding/project/unit_test 08-01） | 任务4 窗口（16:08）前 | ✓ 零改动 |
| `harness/core/*`（api.py 15:54、gate.py 12:44 等全部 ≤15:54） | 任务4 窗口前 | ✓ 零改动 |
| `harness/gate_rules.yaml`（12:21） | 任务4 窗口前 | ✓ 零改动 |
| `harness/dsh/plugins/dashboard-proxy.ts`（09:23 + grep CC = 0） | 任务4 窗口前 | ✓ 零改动 |
| `scripts/attach_dashboard_to_dsh.sh`（08-14 23:07） | 任务4 窗口前 | ✓ 零改动 |
| 任务3 目录历史产物（.gate.json 13:49、评审报告 13:52 等 6 文件） | 13:39-13:59，任务4 窗口前 | ✓ 零改动 |

### 3.6 回归 —— 通过

- 全量回归 `test_harness_dsh_result.txt`：**Ran 222 tests OK** = 212（非 cc 基线）+ 10（cc 8 旧 + 2 新），grep 实证 10 个 cc 用例均在列 —— **220 基线零回归**
- 拓扑 `test_harness_topology_result.txt`：20/20
- node 单测：6/6（评审独立重跑复核）
- `.gate.json`：schema_version=2、run_id=20260815任务4DSH命令名小写修复、total=passed=242、written_by=charter-orchestrator、gate_token 已生成 —— 落闸有效

## 四、合规自查结论

| 检查项 | 结论 |
|---|---|
| 编码规范 | ✓ 本次为 TS/JS/Markdown 文本级替换 + 测试扩展，无新增 Python 源码；test_cc_command.py 首行编码声明、模块 docstring、函数类型标注、命名规范、unittest 框架、正常/反例/边界覆盖、结果落盘 `unit_test/test/` 全部符合 coding.md |
| 逻辑正确性 | ✓ 8 处替换逐处核实无遗漏（含清单外 L14）；命令名/导出形态/注入逻辑零语义变化；无 `*/` 序列破坏注释 |
| 安全 | ✓ 无凭据硬编码、无 SQL、无危险操作；部署为普通 cp 覆盖 |
| 复用 | ✓ 新增用例复用既有 `read_text` / `scan_dev_ref` 辅助与常量，无重复造轮子（constitution 八） |
| 依赖 | ✓ 零新增依赖，requirements.txt 无变更（constitution 三.3） |
| 测试覆盖 | ✓ node 6/6 + Python 222/222 + 拓扑 20/20 = 242/242 落闸；新增 2 用例覆盖正常（正则校验）与反例（零残留扫描）两大类别 |

## 五、验收标准 1-6 逐条对照

| # | 验收项 | 判定 | 证据 |
|---|--------|------|------|
| 1 | charter-command.ts 命令名小写 | **通过** | L24 `const name = "cc";` / L133 `name: "cc",` 匹配 `/^[a-z][a-z0-9_-]*$/u`；grep CC = 0（8 处替换含 L14） |
| 2 | node 单测全过 | **通过** | 6/6 独立重跑全过；含 `cmd.name === "cc"`（L42）与 `/cc` 文本断言（L79） |
| 3 | 回归补强 | **通过** | +2 用例真实断言（正则校验 / 零残留扫描，白名单空集合理）；10/10 落盘；全量 222 含 220 基线零回归 |
| 4 | docs 清零 | **通过** | dsh-migration.md L73/L75 已改 /cc 全文无大写；资源地图.md L84 注记更新到位 |
| 5 | 部署与启动 | **通过** | 部署产物 md5 双侧全同 + 内容一致；cordis.patch.yml 注册核实；dsh web 完整启动成功（npm bin 权限限制已登记） |
| 6 | 不动边界 | **通过** | cc.md / constitution/** / harness/core/* / gate_rules.yaml / dashboard-proxy.ts / attach_dashboard_to_dsh.sh / 任务3 历史产物 mtime 均在任务4 窗口前，零改动 |

## 六、遗留与建议

1. 问题 1/2（见二）均为低严重度流程记录/口径问题，可在后续任务中改进，无需本次返工
2. 任务3 评审遗留「检测模式形态扩展」已由本次新增 `TestNoCcRefInDshAndDocsDomain` 补齐（命令名形态扫描），遗留关闭
3. 建议研发日志（节点5）如实登记：npm bin 权限限制事实、dsh web 以 node bin.js 方式启动的验证记录

---

**评审人**：charter-reviewer ｜ **结论**：REVIEW:APPROVE
