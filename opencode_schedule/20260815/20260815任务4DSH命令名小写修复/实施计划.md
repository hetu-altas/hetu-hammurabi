# 20260815任务4 DSH 命令名小写修复 · 实施计划

> 节点：charter-analyst（分析）｜任务书：`20260815任务4DSH命令名小写修复.md`（167 行）
> 宿主根（HARNESS_DIR）：`/mnt/d/workspace/hetu-altas/hetu-hammurabi`（本项目即宿主，`.opencode/.harness-env` 缺失，按回退规则①动态确认：constitution/constitution.md + .opencode/agents/ + docs/资源地图.md 均存在）
> AETHER_DIR：`/mnt/d/workspace/hetu-altas/hetu-aether`；VENV_BIN：`/mnt/d/workspace/hetu-altas/venv-hetu/bin/python`

---

## 一、任务概述与验收标准

### 1.1 任务概述

修复 DSH 命令插件 `harness/dsh/plugins/charter-command.ts` 命令名大写 `"CC"` 问题（违反 DSH 命令名校验 `/^[a-z][a-z0-9_-]*$/u`，导致 `npm exec @deepseek-ai/dsh web` 启动时报 `command name "CC" must match ...`）。统一改为小写 `cc`（cc = constitution coding，语义不变：`/cc <任务书路径 或 一句话需求>`），并同步单测断言、client.js 注释、docs 引用，扩展回归用例，重新部署 web profile。

**根因**：20260815 任务3（/dev 更名 /cc）将命令名误写为大写 `"CC"`（任务书已核实 8 处残留，勿重复诊断）。

### 1.2 验收标准（6 项）

| # | 验收项 | 判定方式 |
|---|--------|---------|
| 1 | charter-command.ts 命令名小写 | L24 `const name = "cc";`、L133 `name: "cc",`，匹配 `/^[a-z][a-z0-9_-]*$/u`；全文件无 `/CC`、`"CC"`、`命令名：CC` 残留（grep -c "CC" = 0） |
| 2 | node 单测全过 | `node --experimental-strip-types --test harness/dsh/hetu-dashboard/test/command.test.mjs` 6 用例全过，含 `cmd.name === "cc"` 断言、`/cc` 文本断言 |
| 3 | 回归补强 | `unit_test/test_cc_command.py` 新增 ≥2 用例全过（8 旧 + ≥2 新）；全量回归 220/220 不回归 |
| 4 | docs 清零 | `dsh-migration.md` L73/L75 大写 `/CC` 清零；`资源地图.md` L84 注记更新（删除「L73 大写 /CC 保留未误动」，注明命令名统一小写 cc） |
| 5 | 部署与启动 | web profile 重新部署后 md5 与宿主一致；dsh web 启动无命令名校验错误（环境无法启动时如实登记限制，至少保证 cordis 插件加载不再报 `command name "CC"` 错误——编排者已确认降级方案可接受） |
| 6 | 不动边界 | `.opencode/commands/cc.md`、`constitution/**`、`harness/core/*`、`gate_rules.yaml`、`harness/dsh/plugins/dashboard-proxy.ts`、`scripts/attach_dashboard_to_dsh.sh`、历史产物（任务3 目录 .gate.json 等）零改动 |

### 1.3 已核实事实（本节点独立复核，与任务书一致）

- charter-command.ts 170 行，8 处大写残留已逐行核实（L2/L4/L14/L24/L75/L133/L150/L166），**L14 `* 命令名：CC（constitution coding，宪章编程）。` 确认存在**（既有诊断清单未列，勿漏）。
- DSH 校验正则源码实证：`node_modules/@deepseek-ai/dsh-commands/lib/index.js:68` `const COMMAND_NAME = /^[a-z][a-z0-9_-]*$/u;`，L134 抛 `command name "${definition.name}" must match ...` —— 与用户报错逐字一致。
- 部署目标 `/home/qmt/.dsh/profiles/web/plugins/charter-command.ts` 存在，与宿主 md5 全同 `ce0a5f9718b68b31ccd6d95626549829`（实体拷贝部署，非软链）；web profile `cordis.patch.yml` 注册 `id: charter-command` + `config.harnessDir: /mnt/d/workspace/hetu-altas/hetu-hammurabi`（已核实）。
- `scripts/attach_dashboard_to_dsh.sh` 仅部署 dashboard-proxy（L21 PLUGIN_SRC / L101 cp），**charter-command.ts 需单独 cp 部署**。
- `harness/dsh/` 全域 grep `CC`：仅 3 文件命中（charter-command.ts / command.test.mjs / client.js），无其他遗漏；`dashboard-proxy.ts` 0 命中。
- hetu-hammurabi profile（`/home/qmt/.dsh/profiles/hetu-hammurabi/plugins/`）仅有 charter-gate.ts / charter-orchestrator.ts / charter-recorder.ts，无 command 插件。
- **Node 环境（重要发现）**：默认 PATH `node` 为 v18.19.1（`--experimental-strip-types` 报 bad option）；fnm 安装 v24.19.0 于 `/home/qmt/.local/share/fnm/node-versions/v24.19.0/installation/bin/node`，**实测 `--experimental-strip-types` 可用**（已跑通 `node --experimental-strip-types -e` 冒烟）。运行 node 测试必须使用 fnm v24.19.0 全路径或 `fnm exec --using=24.19.0 -- node ...`。
- 基线：`unit_test/test/test_harness_dsh_result.txt` = Ran 220 tests OK；任务3 `.gate.json` 248/248（220 + 拓扑 20 + cc 命令单测 8，run_id=20260815任务3dev命令更名cc）。
- 任务3 评审遗留「检测模式形态扩展」确认存在（评审报告 L21/L69：DEV_REF_PATTERNS 仅 4 形态）。
- `docs/hetu-hammurabi/dsh-migration.md`（94 行）全文 `/CC` 仅 L73/L75 两处，无修订记录区含大写表述（本次**无需白名单**，L73/L75 为正文引用必须改）。
- `unit_test/test_cc_command.py`（328 行）现有 8 用例（8 类各 1 法），已有可复用辅助：`ROOT` / `WORKSPACE_DIR` / `BUSINESS_PROJECTS` / `DEV_REF_PATTERNS` / `REVISION_WHITELIST` / `DOCS_DOMAIN_FILES` / `has_dev_ref()` / `read_text()` / `scan_dev_ref()`；DOCS_DOMAIN_FILES 已含 `docs/hetu-hammurabi/dsh-migration.md`。

---

## 二、涉及文件清单

### 2.1 修改（源码 / 测试 / 文档）

| 文件（相对宿主根） | 改动点 | 类型 |
|---|---|---|
| `harness/dsh/plugins/charter-command.ts` | 8 处替换（详见 §3.1） | 修改 |
| `harness/dsh/hetu-dashboard/test/command.test.mjs` | L38 标题、L42 断言、L79 正则（详见 §3.2） | 修改 |
| `harness/dsh/hetu-dashboard/client.js` | L234 注释 `/CC` → `/cc` | 修改 |
| `docs/hetu-hammurabi/dsh-migration.md` | L73/L75 `/CC` → `/cc` | 修改 |
| `docs/资源地图.md` | L84 注记更新（删除「L73 大写 /CC 保留未误动」，改为「命令名统一小写 cc（DSH 命令名校验要求小写开头）」） | 修改 |
| `unit_test/test_cc_command.py` | 新增 ≥2 用例（两个新测试类，详见 §4.2） | 修改 |
| `unit_test/test/test_cc_command_result.txt` | 测试结果更新（8+2=10 用例全过） | 修改 |
| `unit_test/test/test_harness_dsh_result.txt` | 全量回归 220/220 结果更新 | 修改 |

### 2.2 部署产物（非仓库文件）

| 路径 | 说明 |
|---|---|
| `/home/qmt/.dsh/profiles/web/plugins/charter-command.ts` | cp 宿主修复后文件，md5 核对与宿主一致 |

### 2.3 任务目录（新建）

`opencode_schedule/20260815/20260815任务4DSH命令名小写修复/`：本实施计划 + 评审报告 + 研发日志 + 研发流程状态 + .gate.json。

### 2.4 明确不动

`.opencode/commands/cc.md`（已核实纯小写 cc）、`constitution/**`、`harness/core/**`、`harness/gate_rules.yaml`、`harness/dsh/plugins/dashboard-proxy.ts`（0 命中）、`scripts/attach_dashboard_to_dsh.sh`、任务3 目录全部历史产物。

---

## 三、技术方案要点

### 3.1 charter-command.ts 修复（8 处精确替换，阶段 1）

| 行号 | 现值 | 改为 |
|---|---|---|
| L2 | `* charter-command · 宪章编程 /CC 命令（DSH web GUI 钩子）` | `/cc 命令` |
| L4 | `* 用户在 GUI 输入框输入 \`/CC <任务书路径 或 一句话需求>\`：` | `` `/cc <任务书路径 或 一句话需求>` `` |
| L14 | `* 命令名：CC（constitution coding，宪章编程）。` | `命令名：cc` |
| L24 | `const name = "CC";` | `const name = "cc";` |
| L75 | `用户通过 /CC 命令启动了宪章研发流程。` | `/cc 命令` |
| L133 | `name: "CC",` | `name: "cc",` |
| L150 | `请提供任务书路径或一句话需求：/CC <任务书路径 或 一句话需求>（...）` | `/cc <...>` |
| L166 | `[charter-command] /CC 命令已注册（...）` | `/cc 命令已注册` |

**替换纪律**：
- 逐处精确替换（优先整行 Edit 唯一锚点，避免全局 sed 误伤）；替换后 `grep -c "CC"` 必须为 0。
- **导出形态不变**：L169 `export { name, inject, apply };`、L170 `export default { name, inject, apply };` 原样保留。
- **无 `*/` 序列**：替换内容不引入星号-斜杠序列；替换后注释块完整性（L1-17 头注释）不受影响。
- L75 位于 `buildCharterPrompt` 返回模板数组内（注入模型的编排提示词），改 /cc 后注入提示词语义一致，不影响 command.test.mjs 既有断言（断言未引用 L75 文本形态）。
- 命令名 `cc` 与 `.opencode/commands/cc.md` 双体系同名（cc = constitution coding），语义不变。

### 3.2 command.test.mjs 同步（阶段 2）

| 行号 | 现值 | 改为 |
|---|---|---|
| L38 | `test("插件注册 /CC 命令", ...)` | `test("插件注册 /cc 命令", ...)` |
| L42 | `assert.equal(cmd.name, "CC");` | `assert.equal(cmd.name, "cc");` |
| L79 | `assert.match(result.text, /\/CC/);` | `assert.match(result.text, /\/cc/);` |

其余 4 用例（任务书模式/一句话需求/相对路径/向上探测）不涉 CC 断言，零改动。

### 3.3 client.js 注释（阶段 3，可并行阶段 1）

L234：`// 数据来自 /CC 流程中模型按提示词要求记录的 runlog 事件。` → `/cc 流程`。该文件仅此一处。

### 3.4 docs 同步（阶段 4，可并行阶段 1）

- `docs/hetu-hammurabi/dsh-migration.md` L73/L75：`/CC` → `/cc`（正文引用，非修订记录，必须改；全文核实无其他大写残留，无需白名单）。
- `docs/资源地图.md` L84：原表述「...20260815 任务3 更新：命令引用 /dev 更名 /cc，L73 大写 /CC 保留未误动）」→ 删除「L73 大写 /CC 保留未误动」，改为「命令名统一小写 cc（DSH 命令名校验要求小写开头）」。

### 3.5 test_cc_command.py 扩展（阶段 5，前置：阶段 1-4 完成）

新增 2 个测试类，复用既有 `read_text` / `scan_dev_ref` 辅助与 ROOT 定位，设计见 §4.2。**要点**：
- 命令名提取用正则断言 `re.fullmatch(r"[a-z][a-z0-9_-]*", name)`（Python 侧等价 DSH 校验）；反案例断言 `name = "CC"` / `name: "CC"` 不存在。
- 零残留扫描对 `harness/dsh/` 域（charter-command.ts / client.js / command.test.mjs）+ docs 域（dsh-migration.md）扫描 `/CC`、`"CC"`、`命令名：CC` 形态；**白名单沿用任务3 模式**：仅放行历史修订记录行（本次 dsh-migration.md 无修订记录区，白名单可为空集或仅登记后续发现的历史行；禁止静默删除历史事实）。
- 不破坏既有 8 用例断言（DEV_REF_PATTERNS / REVISION_WHITELIST 不动）。

### 3.6 部署与验证（阶段 6，前置：阶段 2 通过）

```bash
# 部署（实体拷贝覆盖，非软链）
cp harness/dsh/plugins/charter-command.ts /home/qmt/.dsh/profiles/web/plugins/charter-command.ts
# 核对 md5 与宿主一致（修复后为新 md5，两侧必须全同）
md5sum harness/dsh/plugins/charter-command.ts /home/qmt/.dsh/profiles/web/plugins/charter-command.ts

# dsh web 启动验证（降级方案：环境无法启动时如实登记限制）
npm exec @deepseek-ai/dsh web
```

验证结论分级：
1. 首选：dsh web 正常启动，charter-command 插件加载无 `command name "CC"` 错误；
2. 降级（编排者已确认可接受）：尝试 `dsh web --dry-run` / 插件加载日志 / cordis 加载冒烟佐证，并如实登记环境限制于研发日志。

### 3.7 阶段依赖总览（按任务书二）

| 阶段 | 内容 | 前置 |
|---|---|---|
| 1 | charter-command.ts 8 处小写化 | 无 |
| 2 | command.test.mjs 同步 + node 单测通过 | 1 |
| 3 | client.js L234 | 无（并行 1） |
| 4 | docs 两文件 | 无（并行 1） |
| 5 | test_cc_command.py 扩展 + 全量回归 | 1-4（检测面零残留） |
| 6 | 部署 + dsh web 启动验证 | 2 |

---

## 四、单元测试计划

### 4.1 node 单测（阶段 2，必须全过）

```bash
# ⚠ 必须用 fnm v24.19.0（默认 PATH node v18.19.1 不支持 strip-types）
/home/qmt/.local/share/fnm/node-versions/v24.19.0/installation/bin/node \
  --experimental-strip-types --test harness/dsh/hetu-dashboard/test/command.test.mjs
```

6 用例覆盖（修改后）：
1. 插件注册：`cmd.name === "cc"`、description 含「宪章编程（constitution coding）」、handler 为函数
2. 任务书模式：kind=success、followup 注入 user message 且含 `schema_version: 1` / `seal-gate` / `HARNESS_NOTIFY` / 任务书路径
3. 一句话需求模式：需求文本注入且含「节点 -1 需按」
4. 空输入：kind=error 且文本含 `/cc`（L79 断言更新点）
5. 相对路径任务书：相对工作目录解析（`templates/task_book.md`）
6. harness 向上探测：显式 config 缺失时定位宿主

### 4.2 Python 扩展用例（阶段 5，新增 ≥2）

| 新测试类 | 类型 | 断言要点 |
|---|---|---|
| `TestDshPluginCommandNameLowercase`（正常案例） | 正常 | ①charter-command.ts 含 `const name = "cc";` 与 `name: "cc",`；②提取命令名常量后 `re.fullmatch(r"[a-z][a-z0-9_-]*", name)` 匹配；③反案例：`name = "CC"`、`name: "CC"` 形态不存在 |
| `TestNoCcRefInDshAndDocsDomain`（反案例） | 反案例 | 对 `harness/dsh/plugins/charter-command.ts`、`harness/dsh/hetu-dashboard/client.js`、`harness/dsh/hetu-dashboard/test/command.test.mjs`、`docs/hetu-hammurabi/dsh-migration.md` 扫描 `/CC`、`"CC"`、`命令名：CC` 零命中；白名单=历史修订记录行（本次为空集，如扫描发现修订记录区历史行则登记行号并注明原因） |

扩展后 test_cc_command 用例数 = 8 + 2 = **10**。

### 4.3 全量回归（阶段 5）

```bash
/mnt/d/workspace/hetu-altas/venv-hetu/bin/python -m unittest discover -s unit_test -p "test_*.py" -v
```

- `unit_test/test/test_harness_dsh_result.txt`：220/220 不回归（基线 212 + 8 用例零改动）；
- 落闸构成（参照任务3 248/248 模式）：220 + test_cc_command 扩展后 10 + 拓扑 20 = **250/250**（以 .gate.json 落闸实际构成为准）。

### 4.4 运行记录

所有测试输出（node 6 用例、Python 扩展用例、全量回归摘要）记录于任务目录 研发日志.md；result 文件由测试节点落盘更新。

---

## 五、风险与注意事项

| # | 风险 | 影响 | 规避 |
|---|------|------|------|
| 1 | **node 测试依赖 strip-types 环境**：默认 PATH node 为 v18.19.1（实测不支持 `--experimental-strip-types`，报 bad option） | 阶段 2 直接按任务书命令跑会失败 | 必须用 fnm v24.19.0 全路径 `/home/qmt/.local/share/fnm/node-versions/v24.19.0/installation/bin/node`（已实测 strip-types 可用）；在研发日志登记环境事实 |
| 2 | web profile 部署遗漏：实体拷贝非软链，只改宿主不改部署目标则 web 仍报错 | 验收 5 失败 | cp 后必须 `md5sum` 双侧核对（当前全同值 `ce0a5f97...`，修复后为新值且两侧一致）；`attach_dashboard_to_dsh.sh` 不管 charter-command，需单独 cp |
| 3 | docs 修订记录白名单边界：`dsh-migration.md` L73/L75 是**正文引用**非修订记录 | 若误当白名单保留则验收 4 失败 | 必须改为 /cc；白名单仅用于真实历史修订记录行（本次 dsh-migration.md 无此类行），禁止静默删除历史事实 |
| 4 | 不得破坏任务3 产物：`.opencode/commands/cc.md`（已核实纯小写）、`constitution/**`、`harness/core/*`、`gate_rules.yaml`、`dashboard-proxy.ts`、历史 .gate.json、`attach_dashboard_to_dsh.sh` | 验收 6 失败 / 回归回归 | 上述文件零改动；`git status`/`git diff` 复核改动面仅限 §2.1 清单；test_cc_command.py 既有 8 用例与 DEV_REF_PATTERNS/REVISION_WHITELIST 常量不动 |
| 5 | L14 遗漏（既有诊断清单未列） | 验收 1 残留 | 8 处逐行核对，替换后 `grep -c "CC"` 归零兜底 |
| 6 | dsh web 启动受环境限制（端口/守护进程/登录态等）无法实启 | 验收 5 只能降级 | 编排者已确认降级方案可接受：如实登记限制 + cordis 插件加载冒烟佐证（node 加载插件注册不再抛 TypeError 即可证明命令名校验通过） |
| 7 | 替换引入 `*/` 序列破坏头注释 | 语法/注释异常 | 替换文本不含 `*`/`*/` 序列；替换后目检 L1-17 注释块 |
| 8 | 数据安全 | — | 本任务零数据操作（不涉及库/文件数据），无备份需求；部署 cp 覆盖前可先备份目标文件（可选） |

---

## 六、资源匹配清单

| 需求点 | 匹配资源（路径+行号） | 类型 | 依据与偏差 |
|--------|----------------------|------|-----------|
| DSH 命令名校验正则 | `node_modules/@deepseek-ai/dsh-commands/lib/index.js:68`（`COMMAND_NAME = /^[a-z][a-z0-9_-]*$/u`）、`:134`（抛错点，消息与用户报错逐字一致）；`lib/types/index.js:44` 同正则 | 代码（依赖包） | 与任务书正则 `/^[a-z][a-z0-9_-]*$/u` 完全一致，实证来源 |
| 修复目标文件 | `harness/dsh/plugins/charter-command.ts`（170 行，8 处残留 L2/L4/L14/L24/L75/L133/L150/L166） | 代码 | 逐行核实与任务书一致，无偏差；L14 确认存在 |
| node 单测 | `harness/dsh/hetu-dashboard/test/command.test.mjs`（96 行，L38/L42/L79 三处） | 代码（测试） | 行号核实一致；运行需 fnm v24.19.0（默认 node v18.19.1 不支持 strip-types，偏差已登记） |
| client.js 注释 | `harness/dsh/hetu-dashboard/client.js:234` | 代码 | 唯一一处，已核实 |
| docs 引用 | `docs/hetu-hammurabi/dsh-migration.md:73,75`（全文仅此两处）；`docs/资源地图.md:84`（注记含「L73 大写 /CC 保留未误动」） | 文档 | 与任务书一致；dsh-migration.md 无修订记录区含大写表述，无需白名单 |
| 回归用例扩展基线 | `unit_test/test_cc_command.py`（328 行，8 用例；辅助函数 `read_text`/`scan_dev_ref`、常量 `DEV_REF_PATTERNS`/`REVISION_WHITELIST`/`DOCS_DOMAIN_FILES` 可复用） | 代码（测试） | 复用既有扫描形态；新用例按任务书 7.2 设计 |
| 部署注册配置 | `/home/qmt/.dsh/profiles/web/cordis.patch.yml`（`id: charter-command` + `config.harnessDir`）；`/home/qmt/.dsh/profiles/web/plugins/charter-command.ts`（md5 `ce0a5f9718b68b31ccd6d95626549829` 与宿主全同） | 部署配置 | 实体拷贝部署确认；`attach_dashboard_to_dsh.sh` 不含 charter-command（仅 dashboard-proxy），需单独 cp |
| 全量回归基线 | `unit_test/test/test_harness_dsh_result.txt`（Ran 220 tests OK）；任务3 `.gate.json`（248/248，run_id=20260815任务3dev命令更名cc） | 测试基线 | 与任务书一致；落闸构成 220+10+20=250 |
| 任务3 评审遗留 | `opencode_schedule/20260815/20260815任务3dev命令更名cc/评审报告.md` L21/L69（检测模式形态扩展遗留） | 评审记录 | 本次任务 3 补齐该遗留项 |

---

## 七、给编码节点的关键提示

1. **node 测试命令必须使用 fnm v24.19.0 全路径**：`/home/qmt/.local/share/fnm/node-versions/v24.19.0/installation/bin/node --experimental-strip-types --test harness/dsh/hetu-dashboard/test/command.test.mjs`（默认 node v18.19.1 报 bad option，勿用）。
2. **charter-command.ts 共 8 处**（不是 7 处）：L2/L4/L14/L24/L75/L133/L150/L166，替换后 `grep -c "CC"` 必须为 0；导出形态（L169/L170）与 `inject`/`handler` 逻辑零改动；替换内容不得引入 `*/` 序列。
3. **部署是实体拷贝**：`cp harness/dsh/plugins/charter-command.ts /home/qmt/.dsh/profiles/web/plugins/charter-command.ts` 后必须 `md5sum` 双侧核对一致；`scripts/attach_dashboard_to_dsh.sh` 不管此文件，勿依赖该脚本。
4. **dsh-migration.md L73/L75 是正文引用必须改 /cc**（非修订记录，不进白名单）；白名单仅放行真实历史修订记录行。
5. **test_cc_command.py 只增不改旧**：新增两个测试类，复用既有 `read_text`/`scan_dev_ref` 辅助；`DEV_REF_PATTERNS`/`REVISION_WHITELIST`/DOCS_DOMAIN_FILES 常量不动，保证 8 旧用例不回归。
6. **全量回归**：`/mnt/d/workspace/hetu-altas/venv-hetu/bin/python -m unittest discover -s unit_test -p "test_*.py" -v`，220/220 不回归；落闸构成 = 220 + 10（cc 命令扩展后）+ 20（拓扑）。
7. **不动边界零改动**：`.opencode/commands/cc.md`、`constitution/**`、`harness/core/*`、`gate_rules.yaml`、`dashboard-proxy.ts`、`attach_dashboard_to_dsh.sh`、任务3 历史产物；编码后用 `git status`/`git diff` 复核改动面。
8. **dsh web 启动验证允许降级**（编排者已确认）：环境无法实启时登记限制，并可用「node 加载插件注册不再抛 TypeError」作为命令名校验通过的佐证。

---

## 八、给后续节点（测试/评审）的提示

- 测试节点：本实施计划已给出全部运行命令与用例清单；测试通过后仅写 result 文件，落闸由编排器执行（seal-gate，250 构成以 .gate.json 为准）。
- 评审节点：重点核对 §1.2 验收 1（8 处清零 + grep 兜底）、验收 4（docs 两文件）、验收 6（不动边界 git diff 复核）。
- 日志节点：研发日志需含 8 处逐行替换明细（含 L14 清单外发现）、node 测试环境事实（fnm v24.19.0）、部署 md5、dsh web 验证结论（含降级登记）。
