# 评审报告 · 20260815任务2 旧体系门禁升级双体系并行

> 节点：代码评审（charter-reviewer）｜日期：2026-08-15
> 评审范围：`.opencode/plugin/charter-gate.ts`（重写 364 行）+ 文档 6 项 + 测试 3 项 + 回归结果 1 项
> 评审依据：`constitution/constitution.md`、`constitution/coding/coding.md`、`constitution/project/project.md`、任务书、实施计划
> 宿主解析：当前项目 `.opencode/.harness-env` 缺失 → 按回退规则命中 hetu-hammurabi 自身为宿主；AETHER_DIR=hetu-aether；VENV_BIN=venv-hetu

---

## 一、评审结论

**结论：APPROVE（通过）**

旧插件已完整改造为「薄适配新核心」：三个钩子全部委托 Python 核心 CLI（decide/redact/assets-check），旧判定逻辑（全局扫描 gateOpen、硬编码脱敏、函数名通知匹配、行首危险命令匹配）全部删除，fail-closed 兜底齐备；文档「下线/作废/迁移期」零残留、双体系并行表述到位；新增 CLI 契约测试 11 用例全过（≥8 达标），全量回归 212/212 + topology 20/20 = 232/232 落闸；不动边界（constitution/harness/core/cli.py/任务1 产物）经 mtime 窗口复核零触碰。

## 二、问题清单

| # | 位置 | 严重度 | 问题 | 修改建议 |
|---|------|--------|------|---------|
| 1 | `.opencode/plugin/charter-gate.ts` L327-332（chat.message 钩子） | 低 | `redactText` 子进程异常时 `return` 直接跳出循环，后续 text parts 不再脱敏（失败场景下其余 parts 可能含明文凭据；已有 error 告警兜底标注风险，不拦会话符合设计语义） | 建议改为 `continue` 继续处理后续 parts，仅跳过失败 part |
| 2 | `unit_test/test_gate_redact.py` L134/L169（TestRulesSecretPatterns，**既有代码**） | 低 | `open(...)` 未用 `with` 关闭，运行期产生 ResourceWarning（任务1 遗留，不在本次改动范围，未破坏「既有用例零改动」边界） | 后续任务顺手改为 `with open(...)` 即可，本次不动 |
| 3 | 插件加载验证（冒烟记录 L185-186） | 低 | TS 适配层无法自动单测，`charter-gate plugin initialized` 加载日志待**重启 opencode 后人工核查**（冒烟记录已如实注明为遗留事项） | 下一次 opencode 会话重启时核查加载日志，确认无静默加载失败 |
| 4 | `.opencode/plugin/charter-gate.ts` L178（resolveTaskContext 正则） | 低 | 任务目录名匹配 `[^/\\\s]+` 不含空白字符，若未来任务目录名含空格则推导不完整 → 回退 run_id="" 由核心 fail-closed 拦截（不放大风险，仅推导精度损失） | 目录命名契约本就无空格（project.md 4.1），可保持现状，无需修改 |

> 全部为低严重度观察项，无高中严重度问题；均不构成 REVISE 条件。

## 三、逐项检查记录

### 3.1 同构性（旧插件 vs `harness/plugins/charter-gate.ts` 逐段比对）

| 改造段 | DSH 参照段 | 比对结果 |
|--------|-----------|---------|
| `resolveEnv`（L112-134） | DSH L34-49 | 解析逻辑同构（K=V 逐行、跳空行/# 注释、`/^"\|"$/g` 剥引号）；**增强**：HARNESS_DIR 缺失 → `findHostDir` 三要件宿主查找（constitution/constitution.md + .opencode/agents/ + docs/资源地图.md，排序后遍历，宿主自身三要件齐全命中自身，无命中返回当前项目）；VENV_BIN 缺失 → python3 ✓ |
| `decide` 委托（L206-230） | DSH L52-70 | 完全同构：参数顺序、`--secret-file` 拼接 `<HARNESS_DIR>/conf/gate_secret`、`cwd: HARNESS_DIR`、`stdio: ["ignore","pipe","pipe"]`；**增强**：blocked=true 时 CLI 退出码 1 → execFileSync 抛错但 stdout 含判定 JSON，从 `err.stdout` 兜底解析透传 reason/code（GATE_STALE 续签提示依赖此）；stdout 非 JSON → fail-closed 抛错 ✓ |
| `redactText`（L245-252） | DSH L73-86 | 完全同构 ✓ |
| `checkAsset`（L268-276） | DSH L89-103 | 完全同构（--project-dir 传 projectDir）✓ |
| before 钩子（L285-314） | DSH L110-130 | 差异仅在允许范围：opencode 签名 (input, output)、工具名 `input.tool`、参数 `output.args`（已对照 `@opencode-ai/plugin` dist/index.d.ts 类型定义核实 `tool.execute.before` 的 output 含 `args: any`）；日志 `client.app.log`；工具白名单 `["write","edit","apply_patch","bash"]` 一致；GATE_STALE 附 re-seal 提示一致 ✓ |
| chat.message 钩子（L316-339） | DSH L134-149 | 差异仅在允许范围：`output.parts` 可变引用（类型定义核实 `chat.message` 的 output 含 `parts: Part[]`）、告警带 sessionID ✓ |
| after 钩子（L341-360） | DSH L153-162 | 差异仅在允许范围：参数取 `input.args`（类型定义核实 `tool.execute.after` 的 input 含 `args: any`，非 output）；`docs[/\\]hetu-[^/\\]+[/\\]` 正则一致 ✓ |
| 导出形态（L364） | DSH L168 | `export default CharterGate`（opencode Plugin 形态，签名 (client, directory)），与 DSH 的 cordis 导出形态差异属允许范围 ✓ |

### 3.2 fail-closed 兜底核查

| 场景 | 实现 | 结论 |
|------|------|------|
| decide 子进程异常（启动失败/非零退出且 stdout 非 JSON） | L298-302：error 告警 + `throw new Error("[charter-gate] 门禁判定调用失败（fail-closed 拦截）...")` → 硬阻断，**不 fail-open** ✓ | 符合 |
| blocked=true（正常判定拦截） | L304-311：warn 告警 + throw reason（GATE_STALE 附续签提示）✓ | 符合 |
| redact 失败 | L327-332：返回原文 + error 告警「未脱敏原文输出（风险提示）」；不拦会话（chat.message 无阻断语义）✓ | 符合 |
| assets-check 失败 | L354-359：按未登记 warn 软告警（软告警本就只提醒）✓ | 符合 |
| resolveTaskContext 推导不到任务目录 | L184：task_dir=项目目录、run_id="" → 日志/通知判定因 run_id 不匹配被核心拦截（gate.py gate_open 校验链 fail-closed），非日志路径由核心第 4 分支直接放行 ✓ | 符合 |

### 3.3 D1/D2/D4/H5/H6 修复对照

| 缺陷 | 旧判定逻辑（已删除） | 委托后语义 | 结论 |
|------|---------------------|-----------|------|
| D1 串门 | findAllGateFiles/latestGateFile/gateOpen（全局扫描取最新 .gate.json）已删除 | decide 只认当前任务目录 .gate.json + run_id 一致（gate.py L468-521） | ✓ |
| D2 伪造 | 无 token 校验已删除 | gate_token HMAC 校验 + result 摘要 + 新鲜度 10 分钟（GATE_TOKEN_INVALID/GATE_STALE）；冒烟②实测伪造闸被拦 | ✓ |
| D4 绕过 | 函数名通知匹配（util_dingtalk|send_markdown|send_text）与行首 `^\s*(rm\s+-rf|DROP...)` 匹配已删除 | gate_rules.yaml 外置：通知 URL/函数双特征 + 唯一出口 HARNESS_NOTIFY；危险命令任意位置匹配 + 备份语义校验（冒烟③实测 curl 直发被拦） | ✓ |
| H5 硬编码脱敏 | SECRET_PATTERNS 三类正则硬编码已删除 | redact 委托，secret_patterns 段外置 gate_rules.yaml（冒烟⑤实测三类样本全部 [REDACTED]） | ✓ |
| H6 资产回退不全 | resolveResourceMapPath 两级回退已删除（升级为 resolveEnv） | assets-check 委托，宿主→当前项目→缺失三级回退（测试 TestCliAssetsCheck.test_host_map_missing_fallback 覆盖） | ✓ |

### 3.4 安全核查

- 无明文密钥/Token 硬编码（conf/gate_secret 仅以路径拼接传入，内容不落代码）✓
- 块注释内 `*/` 序列：脚本核查（正则提取全部块注释体检查）**零命中**，不会静默截断注释 ✓
- 子进程调用 `execFileSync` 数组传参（无 shell 注入面）、stdin 忽略 ✓
- 无危险命令、无 SQL、无凭据日志输出 ✓

### 3.5 文档核查（验收标准 4）

| 检查项 | 结果 |
|--------|------|
| `docs/` 目录 grep「下线\|作废\|迁移期」 | **零命中**（grep 退出码 1）✓ |
| `docs/harness/extend.md` | 新增 **4.4 双体系并行**章节（L103-116）：两个入口/共享核心/何时用哪个/维护升级规则/与 4.3 衔接，五要素齐全 ✓ |
| `docs/hetu-hammurabi/dsh-migration.md` | 文首引言（L4-7）改双体系并行语境；L57 目录注释改写；**第六节「双体系并行策略」**（L87-94）含两入口/共享核心/何时用哪个/维护规则/任务3 完成注记；第一~五节历史对照保留 ✓ |
| `docs/harness/gates.md` | L27-28 双入口表述 + 补注「旧 opencode 插件已薄适配新核心（20260815 任务2），双体系并行共享判定」✓ |
| `docs/harness/README.md` | L57 组件清单补「双体系并行」说明（两入口共享同一 Python 判定核心与 gate_rules.yaml）✓ |
| `docs/harness/topology.md` | L4 相关实现 + L6 任务2 更新注记 + 六、门禁联动节（L97-101）改写为双体系并行 + 三钩子委托表述 ✓ |
| `docs/资源地图.md` | L83 dsh-migration 条目同步双体系并行语境 + 任务2 注记 ✓ |

### 3.6 测试质量（验收标准 2/3/5）

| 测试文件 | 新增类（用例数） | 覆盖点 | 结论 |
|---------|----------------|--------|------|
| test_gate_bypass.py | TestCliDecide（5） | 出参四字段齐全+退出码 1（未落闸）、合法 v2 闸放行退出码 0、curl 直发被拦（NOTIFY_BLOCKED）、伪造 gate_token 被拦（GATE_TOKEN_INVALID）、run-id 不匹配被拦（GATE_RUN_ID_MISMATCH）——正常/反例/边界齐备 | ✓ |
| test_gate_redact.py | TestCliRedact（3） | 出参 {text,hits} + 三类样本脱敏生效、正常文本零命中原文返回、--json stdout 纯一行 JSON 无 stderr 混杂 | ✓ |
| test_gate_assets.py | TestCliAssetsCheck（3） | 出参 {ok,reason,map_path} + 未登记告警退出码 1、已登记静默退出码 0、宿主资源地图缺失回退当前项目 | ✓ |

- 新增合计 **11 用例 ≥ 8** ✓；子进程调用风格与 test_gate_concurrency.py 一致（sys.executable -m harness.core.cli + cwd=宿主根 + timeout=60）✓
- **既有用例零改动**：TestCli* 均为文件末尾独立新增类（既有类 TestGateLogRules/TestGateNotifyRules/TestGateDestructiveRules/TestGateDecide/TestGateBypassVariants/TestGateLogAllowlist/TestRedactSecrets/TestRulesSecretPatterns/TestResolveResourceMapPath/TestCheckRegistered 内容完整未动）✓
- 回归：**212/212**（201 基线 + 11 新增）+ topology **20/20** = 232/232，`.gate.json` v2 已落闸（schema_version=2、run_id 一致、total=passed=232、gate_token 已签名、written_by=charter-orchestrator）✓

### 3.7 不动边界复核（mtime 窗口 + git 状态）

- 2026-08-15 13:00 后修改文件与任务2 清单**精确吻合**：charter-gate.ts、docs 6 项、测试 3 + 结果 1、任务目录流程产物（任务书/实施计划/冒烟记录/流程状态/.gate.json）✓
- `constitution/**`、`harness/core/*`、`harness/gate_rules.yaml`、`harness/plugins/*`、`harness/core/cli.py`、20260814/20260815 任务1 产物、业务项目代码：**零改动**（git status 显示的 constitution 等 M 状态为历史未提交改动，mtime 均早于本任务窗口，非本任务所为）✓

### 3.8 冒烟验证（验收标准 6）

冒烟 5 项全过（冒烟验证记录.txt）：
- ① 门禁未开写日志被拦（GATE_MISSING）✓
- ② 伪造 .gate.json 两个样本不开闸（GATE_NO_RESULTS / GATE_TOKEN_INVALID）✓
- ③ curl 直发钉钉被拦（NOTIFY_BLOCKED）✓
- ④ 合法 v2 闸放行（任务1 目录因全量回归更新 result 触发防篡改防线拒绝续签，改走 temp 合法闸等价验证，机制事实如实记录）✓
- ⑤ redact 三类样本脱敏 hits=3 + assets-check 未登记软告警/非目标路径跳过 ✓

## 四、合规自查结论

| 宪章条款 | 检查结果 |
|---------|---------|
| 安全底线（constitution.md 一） | 无明文密钥/Token、无危险命令、无 SQL 拼接 ✓ |
| 编码要求（四） | TS 适配层类型标注/注释纪律符合（Python 侧既有代码未动）✓ |
| 单元测试（六） | 新增 11 用例覆盖正常/反例/边界，unittest 框架，结果落 unit_test/test/ ✓ |
| 代码复用（八） | 无重复实现：全部委托 harness 核心，不重复造轮子 ✓ |
| 文件与目录（九） | 无新增目录，命名合规 ✓ |
| 研发流程（十三.7 门禁） | .gate.json v2 落闸 232/232，charter-gate 硬拦截已生效 ✓ |
| AI 分析范式（十四） | 本任务为机制适配（meta task），无分析判断逻辑 ✓ |

## 五、验收标准 1-6 逐条对照

| # | 验收项 | 结论 | 依据 |
|---|--------|------|------|
| 1 | 契约一致：旧插件委托 decide 判定与 DSH 侧完全一致 | **通过** | 两插件调用同一 `harness.core.cli decide` 同参数契约（逐段比对同构）；冒烟输出 JSON 与 DSH 行为一致 |
| 2 | 伪造不开闸（GATE_TOKEN_INVALID 等） | **通过** | 冒烟②两样本（无 result_files/伪造 token）+ TestCliDecide.test_forged_gate_token_invalid |
| 3 | curl 直发被拦（NOTIFY_BLOCKED） | **通过** | 冒烟③ + TestCliDecide.test_curl_direct_blocked |
| 4 | 文档零残留 + 双体系并行表述 | **通过** | docs/ grep「下线\|作废\|迁移期」零命中；extend 4.4 章节、dsh-migration 第六节、gates L27、README L57、topology、资源地图 L83 全部到位 |
| 5 | 回归全绿 201/201 不回归 + 新契约测试 ≥8 | **通过** | 全量 **212/212**（201 基线 + 11 新增契约用例 ≥8）+ topology 20/20 |
| 6 | 冒烟 5 项全过 | **通过** | 冒烟验证记录.txt ①-⑤ 全部按预期（含两个额外机制事实记录） |

## 六、遗留事项

1. 重启 opencode 后人工核查插件加载日志（`charter-gate plugin initialized`），确认无静默加载失败（Node v18.19.1 无法自动单测 TS，属降级方案已批准范围）。
2. 低严重度观察项（见问题清单 #1/#2/#4）可择机改进，不阻塞本次交付。

---
*评审结论：REVIEW:APPROVE*
