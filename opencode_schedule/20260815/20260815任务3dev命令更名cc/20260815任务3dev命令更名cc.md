# 20260815任务3 dev命令更名cc

## 重要提示
通用宪章规范统一存放在 harness 宿主项目（运行期由当前项目 `.opencode/.harness-env` 的 HARNESS_DIR 定位，缺失时按回退规则查找：同父目录下同时含 `constitution/constitution.md` + `.opencode/agents/` + `docs/资源地图.md` 的 hetu-* 目录即宿主）的 constitution 目录中，以 `constitution/constitution.md` 为顶层约束，任务执行前请务必仔细阅读并严格遵守。
宪章编程的流程编排与各节点规范见 harness 宿主的 `.opencode` 与 `templates` 目录。

> **宿主解析**：当前项目 `.opencode/.harness-env` 缺失（hetu-hammurabi 为宿主自身，未生成该文件），按回退规则命中宿主即 hetu-hammurabi 自身：`<HARNESS_DIR>` = `/mnt/d/workspace/hetu-altas/hetu-hammurabi`；`<AETHER_DIR>` = `/mnt/d/workspace/hetu-altas/hetu-aether`；`<VENV_BIN>` = `/mnt/d/workspace/hetu-altas/venv-hetu/bin/python`；业务项目 = `/mnt/d/workspace/hetu-altas/{hetu-aether,hetu-mercury,hetu-sybil,hetu-thoth}`（均实测存在）。
> 本任务为**机制自优化任务（meta task）**：改造对象即 opencode 入口命令 `/dev` 本身。下述引用行号均已在需求分析期逐条核实真实存在（sed 逐行核对 + 全仓 grep 兜底扫描，排除设备路径 `/dev/urandom` 等噪音）。

---

## 一、任务要求

### 需求背景

一句话需求：「把/dev命令改成/cc，意思就是 constitution coding」。

opencode 命令名 = `.opencode/commands/<文件名>`，改命令即改文件名。当前命令文件为宿主 `.opencode/commands/dev.md`（970 字节，13 行，已被 git 跟踪），命令引用散布于配置域、脚本域、宪章域、文档域（docs/harness/、docs/hetu-hammurabi/、docs/hetu-sybil/）、入门文档共 18 个文件 41 处，另 2 处修订记录（历史事实）默认保留。

**关键核实结论（推翻"4 项目逐个删/建"原方案）**：4 个业务项目（hetu-aether/hetu-mercury/hetu-sybil/hetu-thoth）的 `.opencode/commands` 目录均为**目录级软链**，指向宿主 `/mnt/d/workspace/hetu-altas/hetu-hammurabi/.opencode/commands`（由 `scripts/install_harness.sh` L19/L81 安装：`LINK_ITEMS=(agents commands skills plugin ...)` + `ln -sfn`）。业务项目路径下的 `dev.md` 即为宿主实体文件经软链可见（md5 全同 `92e5323c...` = 同一 inode）。因此**只需改宿主文件**，4 个项目自动同步生效；逐个删/建实体文件反而破坏软链共享机制、造成后续同步脱节。

### 目标

1. **命令更名**：宿主 `.opencode/commands/dev.md` → `cc.md`（git mv 保留历史），内容更新：description 与正文中 `/dev` → `/cc`，并在文件内注明「cc = constitution coding」含义。
2. **引用更新**：按下方「三、文件与目录」清单逐文件更新（`/dev` → `/cc`、`dev.md` → `cc.md`），范围覆盖配置域 / 脚本域 / 宪章域 / 文档域 / 入门文档 / 业务设计文档；设备路径（`/dev/urandom`、`/dev/null`）明确不动。
3. **可测性设计**：新建 `unit_test/test_cc_command.py`（≥6 用例），固化更名正确性断言（详见「七、单元测试」）。
4. **文档语义**：README / 快速上手指南 / 宪法第十三条第 1 款中 cc 含义说明到位。

### 验收标准（可逐条验证）

1. **命令就位**：宿主 `.opencode/commands/cc.md` 存在，内容含「cc = constitution coding」含义说明与 `/cc` 用法，且无遗留 `/dev` 命令引用；4 个业务项目经目录软链自动可见同一 cc.md（内容与宿主一致）；各项目 `dev.md` 已不存在。
2. **引用清零**：全仓（排除 `opencode_schedule/` 历史产物、文档修订记录行、设备路径）grep「`/dev <`」与「`commands/dev.md`」**零命中**；「`/cc`」引用到位（配置域 / 宪章 L128 / docs/harness / docs/hetu-hammurabi / docs/hetu-sybil / README 双版 / quick_start / 快速上手指南）。
3. **测试门禁**：新增 `unit_test/test_cc_command.py` ≥6 用例全部通过（结果落 `unit_test/test/test_cc_command_result.txt`）+ 全量回归不回归（基线 212/212 + topology 20/20 = 落闸 232/232，以实测为准）。
4. **含义说明**：README.md / 快速上手指南.md / `constitution/constitution.md` L128 中 cc = constitution coding 说明到位。

### 不动边界（明确红线）

- `constitution/constitution.md` **仅 L128（第十三条第 1 款）一处命令引用更新**，其余零改动（宪法为权威规范，命令名变更属必要修订）。
- `opencode_schedule/` 历史任务产物（任务书/日志/评审报告）中的 /dev 为历史记录，**不动**。
- `harness/core/*`（含 secret.py，已核实无命令引用）、`harness/gate_rules.yaml`（已核实无命中）、`harness/plugins/*`、`harness/workflow.yaml`、`scripts/attach_native_panel.sh`（已核实无命令引用）**零改动**。
- `docs/harness/gates.md`（已核实仅设备路径 `/dev/urandom`，无命令引用）**不动**。
- 设备路径 `/dev/urandom`、`/dev/null`、`/dev/tcp`、`/dev/std*` 等**一律不动**。
- 修订记录行（docs/hetu-sybil/架构与模块设计.md L732、docs/hetu-sybil/研发计划.md L661）：历史事实记录，默认**保留原文不动**（与 opencode_schedule/ 历史产物同理，保真历史），待确认项 T3 复核。

---

## 二、拆分策略

单阶段小任务（代码量 ≤300 行，不拆分任务），内部按顺序执行：

| 子步骤 | 内容 | 完成标志 |
|--------|------|---------|
| A 命令更名 | git mv 宿主 dev.md → cc.md，内容更新（/dev→/cc + cc 含义说明） | 5 项目路径下 cc.md 可见、dev.md 消失 |
| B 引用更新 | 按清单 18 文件 41 处更新（/dev→/cc、dev.md→cc.md） | 全仓 grep 验证零命中（排除不动域） |
| C 测试验证 | 新增 test_cc_command.py ≥6 用例 + 全量回归 | 单测全过 + 基线不回归 |

## 三、文件与目录

### 3.1 命令更名（1 个文件操作，4 项目经软链自动同步）

| 文件 | 说明 | 类型(新建/修改) |
|------|------|------|
| `hetu-hammurabi/.opencode/commands/dev.md` → `cc.md` | 入口命令更名 + 内容更新（description、正文 /dev→/cc，注明 cc = constitution coding；git mv 保留历史；4 业务项目经目录软链自动生效，**禁止**逐项目删/建实体文件） | 修改(重命名) |

### 3.2 引用更新（18 文件 41 处，行号已逐条核实）

**配置域（3 文件 3 处）**

| 文件 | 行号 | 现内容 | 改为 |
|------|------|--------|------|
| `.opencode/agents/charter-orchestrator.md` | L14 | 「- `/dev` 命令的 $ARGUMENTS：任务书文件路径 或 一句话需求。」 | `/cc` |
| `harness/agents/charter-orchestrator.md` | L14 | 同上 | `/cc` |
| `.opencode/plugin/charter-gate.ts` | L4 | 注释「opencode 入口（/dev，本文件）与 DSH 入口」 | `/cc` |

**脚本域（2 文件 3 处）**

| 文件 | 行号 | 现内容 | 改为 |
|------|------|--------|------|
| `scripts/install_harness.sh` | L109 | echo 提示「输入 /dev <任务书路径>」 | `/cc <任务书路径>` |
| `scripts/run_charter.sh` | L3 | 注释「（/dev 等价物，C1）」 | `/cc` |
| `scripts/run_charter.sh` | L59 | 注释「（与 /dev 一致：任务书 / 一句话需求 / 空=列出）」 | `/cc` |

**宪章域（1 文件 1 处）**

| 文件 | 行号 | 现内容 | 改为 |
|------|------|--------|------|
| `constitution/constitution.md` | L128（第十三条第 1 款） | 「在业务项目内使用 `/dev <任务书路径>` 命令触发 charter-orchestrator 编排」 | `/cc <任务书路径>`（可顺带括注 cc = constitution coding，保持语义不变） |

**文档域 docs/harness/（6 文件 10 处）**

| 文件 | 行号 | 现内容 | 改为 |
|------|------|--------|------|
| `docs/harness/agents-skills.md` | L60 | 「`.opencode/commands/dev.md`：」 | `cc.md` |
| `docs/harness/agents-skills.md` | L79 | 「\| 任务书路径 \| /dev 输入 \| analyst \|」 | `/cc` |
| `docs/harness/extend.md` | L54 | 「/dev opencode_schedule/...」示例 | `/cc` |
| `docs/harness/extend.md` | L109 | 「opencode 入口：`.opencode/plugin/charter-gate.ts`（/dev 流程，已薄适配新核心...）」 | `/cc` |
| `docs/harness/README.md` | L29 | 架构图「/dev <任务书> ────────▶」 | `/cc` |
| `docs/harness/README.md` | L53 | 「\| 入口命令 \| `.opencode/commands/dev.md` \| `/dev <任务书>` 触发编排 \|」 | `cc.md` / `/cc` |
| `docs/harness/README.md` | L70 | 「输入: /dev <任务书路径 或 一句话需求>」 | `/cc` |
| `docs/harness/workflow.md` | L10 | 同上 | `/cc` |
| `docs/harness/topology.md` | L128 | FAQ「重启 opencode 验证 `/dev`」 | `/cc` |

**文档域 docs/hetu-hammurabi/（1 文件 2 处）**

| 文件 | 行号 | 现内容 | 改为 |
|------|------|--------|------|
| `docs/hetu-hammurabi/dsh-migration.md` | L68 | 「# 启动一次研发（业务项目目录内，/dev 等价物 C1）」 | `/cc` |
| `docs/hetu-hammurabi/dsh-migration.md` | L91 | 「opencode 入口 `.opencode/plugin/charter-gate.ts`（/dev 流程）已升级为薄适配层」 | `/cc` |

**文档域 docs/hetu-sybil/（2 文件 14 处，其中 2 处修订记录默认保留）**

| 文件 | 行号 | 现内容 | 改为 |
|------|------|--------|------|
| `docs/hetu-sybil/架构与模块设计.md` | L66 | 「通过 `/dev <任务书路径>` 触发（`hetu-hammurabi/.opencode/commands/dev.md`）」 | `/cc` + `cc.md` |
| `docs/hetu-sybil/架构与模块设计.md` | L158 | 「研发期经 harness `/dev` 命令触发开发流程」+「`hetu-hammurabi/.opencode/commands/dev.md`（研发入口）」 | `/cc` + `cc.md` |
| `docs/hetu-sybil/架构与模块设计.md` | L165 | 「harness 共享底座（charter-*、`/dev`、charter-gate）不变」 | `/cc` |
| `docs/hetu-sybil/架构与模块设计.md` | L428 | 「研发期经 `/dev <任务书>` 触发开发流程」 | `/cc` |
| `docs/hetu-sybil/架构与模块设计.md` | L456 | 「与 `/dev` 同构；批量 ≠ 多次实例化」 | `/cc` |
| `docs/hetu-sybil/架构与模块设计.md` | L514 | 「由 `/dev` 入口（`.opencode/commands/dev.md`）触发」 | `/cc` + `cc.md` |
| `docs/hetu-sybil/架构与模块设计.md` | L516 | 「harness 共享底座（charter-* 研发流程代理/技能、charter-gate 插件、`/dev` 命令）保持 hammurabi 共享机制不变」 | `/cc` |
| `docs/hetu-sybil/架构与模块设计.md` | L522 | 「...`/dev` 命令）保持 hammurabi 共享机制不变 | 备选已否...」 | `/cc` |
| `docs/hetu-sybil/架构与模块设计.md` | L581 | 「（研发期 `/dev` 仅用于开发，运行期用私有入口）」 | `/cc` |
| `docs/hetu-sybil/架构与模块设计.md` | L671 | 「\| 命令 \| `/dev`（hammurabi 共享，研发入口）\| 私有命令 `/analyze`...」 | `/cc` |
| `docs/hetu-sybil/架构与模块设计.md` | L694 | 「\| 命令/模板 \| `hetu-hammurabi/.opencode/commands/dev.md`、`templates/task_book.md` \| 入口与模板 \|」 | `cc.md`（兜底扫描发现，用户清单未列，默认纳入更新，待确认项 T2） |
| `docs/hetu-sybil/架构与模块设计.md` | L732 | 修订记录「...与 /dev 一任务一流水线同构...」（2026-08-12 历史） | **默认保留不动**（待确认项 T3） |
| `docs/hetu-sybil/研发计划.md` | L460 | 「在 hetu-sybil 项目内使用 `/dev <任务书路径>` 命令触发...（`hetu-hammurabi/.opencode/commands/dev.md`；宪章第十三条第 1 款）」 | `/cc` + `cc.md` |
| `docs/hetu-sybil/研发计划.md` | L503 | 「研发入口 `/dev` 不变」 | `/cc` |
| `docs/hetu-sybil/研发计划.md` | L633 | 「...`.opencode/commands/dev.md`、`scripts/install_harness.sh` \| 全部/任务 11」 | `cc.md` |
| `docs/hetu-sybil/研发计划.md` | L661 | 修订记录「...与 /dev 同构...」（2026-08-12 历史） | **默认保留不动**（待确认项 T3） |

**入门文档（4 文件 8 处）**

| 文件 | 行号 | 现内容 | 改为 |
|------|------|--------|------|
| `README.md` | L70 | 目录树「└── dev.md  # /dev 入口命令」 | `cc.md` + `/cc` |
| `README.md` | L119 | 「/dev <任务书路径 或 一句话需求>」 | `/cc` |
| `README.en.md` | L70 | 目录树「└── dev.md  # /dev entry ...」 | `cc.md` + `/cc` |
| `README.en.md` | L121 | 「/dev <task book path 或 one-line requirement>」 | `/cc` |
| `quick_start.md` | L35 | 「/dev implement a generic utility...」 | `/cc` |
| `quick_start.md` | L47 | 「/dev opencode_schedule/...」 | `/cc` |
| `快速上手指南.md` | L35 | 「/dev 实现一个通用工具方法...」 | `/cc` |
| `快速上手指南.md` | L47 | 「/dev opencode_schedule/...」 | `/cc` |
| `快速上手指南.md` | L70 | FAQ「换机器后 /dev 不可用」 | `/cc` |

### 3.3 单元测试

| 文件 | 说明 | 类型(新建/修改) |
|------|------|------|
| `unit_test/test_cc_command.py` | 命令更名正确性断言（≥6 用例，详见第七章） | 新建 |

### 3.4 资产沉淀（执行后更新，见第九章）

| 文件 | 说明 | 类型 |
|------|------|------|
| `<HARNESS_DIR>/docs/资源地图.md` | 若涉命令路径描述，按需同步（当前资源地图未见 dev.md 命令引用，预计零改动，仅核验） | 核验 |

## 四、接口 / 数据格式定义

- **命令契约（改名前后一致，仅名称变化）**：
  - 输入：`$ARGUMENTS` = 任务书文件路径 或 一句话需求（或空 = 列出可用任务书）
  - 触发：`/cc <任务书路径>` → charter-orchestrator 节点 0；`/cc <一句话需求>` → 节点 -1 先生成任务书；`/cc`（空）→ 扫描 `opencode_schedule/` 与 `docs/` 列出任务书
  - 输出：任务目录 `opencode_schedule/<YYYYMMDD>/<任务目录>/研发流程状态.md` 逐节点状态固化
- **cc.md 文件格式**（沿用 dev.md 结构）：frontmatter（description / agent: charter-orchestrator）+ 输入判别三模式 + 执行要求；description 中注明「cc = constitution coding（宪章编程）」
- **关键字段**：命令名 `cc`、代理 `charter-orchestrator`、判别模式 3 种（任务书路径/一句话需求/空输入）

## 五、Shell 脚本

本任务不新增业务脚本；涉及命令操作为：

```bash
# A 子步骤：更名（保留 git 历史）
cd /mnt/d/workspace/hetu-altas/hetu-hammurabi
git mv .opencode/commands/dev.md .opencode/commands/cc.md
# 编辑 cc.md：description 与正文 /dev→/cc，注明 cc = constitution coding
# 4 业务项目经目录软链自动同步，禁止逐个删/建实体文件

# B 子步骤：引用更新后全仓核验（排除不动域）
grep -rn --include="*.md" --include="*.ts" --include="*.py" --include="*.sh" --include="*.yaml" \
  -e "/dev <" -e "commands/dev.md" -e "commands/dev\." \
  /mnt/d/workspace/hetu-altas/hetu-hammurabi \
  --exclude-dir=opencode_schedule --exclude-dir=node_modules --exclude-dir=.git 2>/dev/null | grep -v "dev.md~" || echo "零命中"
# 注意：修订记录行（架构 L732、研发计划 L661）默认保留，核验时按白名单放行

# C 子步骤：测试
/mnt/d/workspace/hetu-altas/venv-hetu/bin/python -m unittest unit_test.test_cc_command -v
# 全量回归（基线 212/212 + topology 20/20，以实测为准）
```

## 六、产出

- **输出路径**：
  - 命令文件：`/mnt/d/workspace/hetu-altas/hetu-hammurabi/.opencode/commands/cc.md`（4 业务项目经软链可见）
  - 测试脚本：`/mnt/d/workspace/hetu-altas/hetu-hammurabi/unit_test/test_cc_command.py`
  - 测试结果：`/mnt/d/workspace/hetu-altas/hetu-hammurabi/unit_test/test/test_cc_command_result.txt`
- **数据库/日志落点**：无（纯文件与文档变更，不涉数据库）

## 七、单元测试

测试文件：`unit_test/test_cc_command.py`（unittest 风格，参照 `test_harness_topology.py` 结构；引入路径用 `os.path.dirname(os.path.abspath(__file__))` 相对定位，结果保存到 `unit_test/test/` 下并输出为 `test_cc_command_result.txt`）。

必须覆盖（正常 / 反案例 / 边界条件，≥6 用例）：

| 用例 | 类型 | 断言内容 |
|------|------|---------|
| `test_host_cc_exists_and_content` | 正常 | 宿主 `.opencode/commands/cc.md` 存在；内容含「constitution coding」含义说明；含 `/cc` 用法（`$ARGUMENTS` 判别三模式）；**不含**遗留 `/dev` 命令引用（description 与正文） |
| `test_business_projects_cc_consistent` | 正常 | 4 个业务项目（hetu-aether/hetu-mercury/hetu-sybil/hetu-thoth）路径下 `.opencode/commands/cc.md` 内容与宿主完全一致（经目录软链自动同步） |
| `test_dev_md_removed` | 正常 | 宿主与 4 业务项目路径下 `.opencode/commands/dev.md` 均不存在 |
| `test_no_dev_ref_in_config_domain` | 反案例 | 配置域（`.opencode/agents/charter-orchestrator.md`、`harness/agents/charter-orchestrator.md`、`.opencode/plugin/charter-gate.ts`、`scripts/install_harness.sh`、`scripts/run_charter.sh`）grep「`/dev`」命令引用零命中（排除注释外设备语义由人工复核） |
| `test_no_dev_ref_in_docs_domain` | 反案例 | 文档域（`constitution/constitution.md`、`docs/harness/*.md`、`docs/hetu-hammurabi/*.md`、`docs/hetu-sybil/*.md`、根 `README.md`/`README.en.md`、`quick_start.md`、`快速上手指南.md`）grep「`/dev <`」与「`commands/dev.md`」零命中；白名单放行：修订记录行（架构 L732、研发计划 L661）、设备路径（`/dev/urandom` 等） |
| `test_constitution_updated` | 正常 | `constitution/constitution.md` L128 含「/cc <任务书路径>」且不含「/dev <任务书路径」命令引用；宪法其余部分无命令引用变更 |
| `test_cc_usage_documented` | 正常 | 根 `README.md`、`快速上手指南.md` 含 `/cc` 引用与 cc = constitution coding 含义说明 |
| `test_detector_positive`（可选） | 边界 | 构造临时含「/dev <」与「commands/dev.md」的模拟文件，断言检测逻辑能识别（自检测试器有效性） |

## 八、研发日志

记录在任务目录 `opencode_schedule/20260815/20260815任务3dev命令更名cc/` 下（研发日志 + 研发流程状态 + 评审报告 + 实施计划）。

## 九、资产沉淀

本次研发需沉淀/更新的 docs 文档（存于 harness 宿主 `<HARNESS_DIR>/docs/`）：

- 更新：`docs/harness/README.md`、`docs/harness/agents-skills.md`、`docs/harness/extend.md`、`docs/harness/workflow.md`、`docs/harness/topology.md`（命令引用 /dev → /cc，即本任务引用更新本身，登记变更说明）
- 更新：`docs/hetu-hammurabi/dsh-migration.md`（/dev → /cc 同步）
- 更新：`docs/hetu-sybil/架构与模块设计.md`、`docs/hetu-sybil/研发计划.md`（正文 /dev → /cc；修订记录行默认保留）
- 核验：`docs/资源地图.md`（当前无 dev.md 命令引用，预计零改动；如核验命中则同步登记）
- 任务完成后按 charter-assets 流程在资源地图登记本任务变更说明（docs/harness/README.md 等条目追加「20260815 任务3 更新：/dev 更名 /cc」注记）。

## 十、通知

使用公共工具项目（`<AETHER_DIR>` = `/mnt/d/workspace/hetu-altas/hetu-aether`）`utils.util_dingtalk.send_markdown` 发送完成通知。

---

## 待确认项（执行前请编排者确认，默认值见各条目）

- **T1（关键设计变更）**：业务项目 `.opencode/commands` 为**目录级软链**（install_harness.sh L81 安装），更名仅需改宿主、4 项目自动同步——原方案「4 项目删除 dev.md、放入 cc.md」**已核实不必要**（软链目录下逐个建实体文件反而破坏共享机制）。默认按「仅改宿主 + 软链同步」执行；若确需业务项目实体独立文件（如未来任务 11 目录混合化前置），请明确指示。
- **T2**：`docs/hetu-sybil/架构与模块设计.md` L694（资源索引表「命令/模板 \| ...commands/dev.md」）为兜底扫描发现、不在原始清单内，但属**当前资源索引非修订记录**，默认纳入更新（dev.md → cc.md）。
- **T3**：修订记录行（架构 L732、研发计划 L661）中的 /dev 属历史事实记录，默认**保留不动**（与 opencode_schedule/ 历史产物同理）；如要求全量统一（含修订记录）请确认，届时同步调整单测用例白名单。
- **T4**：`scripts/install_harness.sh` 仅 L109 一处 echo 提示更新；安装逻辑（LINK_ITEMS 目录软链）无需改动（cc.md 与 dev.md 同目录，自动覆盖）。如后续需要安装脚本将 dev.md 排除/特判，另行评估，本次不动。
