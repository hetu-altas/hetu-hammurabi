# 20260815 任务3 dev命令更名cc · 实施计划

> 产出节点：charter-analyst（分析节点）
> 任务书：`opencode_schedule/20260815/20260815任务3dev命令更名cc/20260815任务3dev命令更名cc.md`
> 宿主：`/mnt/d/workspace/hetu-altas/hetu-hammurabi`（自宿主，`.harness-env` 不存在，回退规则命中自身）
> 本任务为**机制自优化任务（meta task）**：改造对象即 opencode 入口命令 `/dev` 本身。

---

## 一、任务概述与验收标准

### 1.1 任务概述

将 opencode 入口命令 `/dev` 更名为 `/cc`（cc = constitution coding，宪章编程）：

- **A 命令更名**：宿主 `.opencode/commands/dev.md` → `cc.md`（git mv 保留历史），内容更新并注明 cc 含义。
- **B 引用更新**：全仓 18 文件引用更新（`/dev` → `/cc`、`dev.md` → `cc.md`），排除设备路径与修订记录。
- **C 测试验证**：新建 `unit_test/test_cc_command.py`（≥6 用例）+ 全量回归。

**关键机制（已实测核实）**：4 个业务项目（hetu-aether/hetu-mercury/hetu-sybil/hetu-thoth）的 `.opencode/commands` 均为**目录级软链**指向宿主同名目录（`lrwxrwxrwx ... commands -> /mnt/d/workspace/hetu-altas/hetu-hammurabi/.opencode/commands`，由 `scripts/install_harness.sh` L18-19 `LINK_ITEMS=(agents commands skills plugin ...)` + L81-83 `ln -sfn` 安装）。5 路径下 `dev.md` md5 全同 `92e5323ced25f8873e6e49a3a852a270`、inode 全同 `1407374883587261`（同一实体文件）。**因此只需改宿主文件，4 项目自动同步**；禁止逐个删/建实体文件（会破坏软链共享机制）。

### 1.2 验收标准（4 项，可逐条验证）

| # | 验收标准 | 验证方式 |
|---|---------|---------|
| 1 | 命令就位：宿主 `.opencode/commands/cc.md` 存在，内容含「cc = constitution coding」含义说明与 `/cc` 用法（$ARGUMENTS 三模式判别），无遗留 `/dev` 引用；4 业务项目经软链自动可见同一 cc.md；各项目 `dev.md` 不存在 | `ls` + 内容检查 + 单测 `test_host_cc_exists_and_content` |
| 2 | 引用清零：全仓（排除 `opencode_schedule/`、修订记录白名单、设备路径）grep 命令引用零命中；`/cc` 引用到位（配置域/宪章 L128/docs/harness/docs/hetu-hammurabi/docs/hetu-sybil/README 双版/quick_start/快速上手指南） | 下方核验 grep 命令 + 单测反案例 |
| 3 | 测试门禁：`unit_test/test_cc_command.py` ≥6 用例全部通过，结果落 `unit_test/test/test_cc_command_result.txt`；全量回归不回归（基线：212 全量 + topology 20 = 232/232，以实测为准） | unittest 运行 + 结果文件 |
| 4 | 含义说明：README.md / 快速上手指南.md / `constitution/constitution.md` L128 中 cc = constitution coding 说明到位 | grep + 单测 `test_cc_usage_documented` |

### 1.3 不动边界（红线）

- `constitution/constitution.md` **仅 L128 一处**更新，其余零改动。
- `opencode_schedule/` 历史产物（含本任务目录外的任务1/2 产物）**不动**。
- `harness/core/*`（含 secret.py，已核实无命令引用）、`harness/gate_rules.yaml`、`harness/plugins/*`、`harness/workflow.yaml`、`scripts/attach_native_panel.sh`、`templates/task_book.md`、`宪章编程宣言.md`、`manifesto.md`、`docs/harness/assets.md` **零改动**（兜底扫描均无命令引用命中）。
- `docs/harness/gates.md`（L88 仅 `/dev/urandom` 设备路径）、`docs/hetu-hammurabi/hard-gate-optimize.md`（L128 仅 `/dev/urandom`）、`scripts/run_charter.sh` L180（`>/dev/null`）**不动**。
- 设备路径 `/dev/urandom`、`/dev/null`、`/dev/tcp`、`/dev/std*`、`/dev/shm` 一律不动。
- **修订记录白名单**（历史事实，保留原文）：`docs/hetu-sybil/架构与模块设计.md` L732、`docs/hetu-sybil/研发计划.md` L661（编排者 T3 已确认）。
- **20260815 任务1/2 未提交产物不得破坏**：git 工作区现有 45 个未提交修改（`.opencode/agents/*`、`.opencode/plugin/charter-gate.ts`、`.opencode/skills/*`、`constitution/*`、`docs/harness/*`、`docs/资源地图.md`、`templates/task_book.md` 等），本任务只做**文件系统级改动**，不提交 git、不 `git add .`、不 `git checkout`/`git stash` 任何文件。

---

## 二、涉及文件清单

### 2.1 命令更名（1 个文件操作）

| 文件 | 操作 | 说明 |
|------|------|------|
| `.opencode/commands/dev.md` → `cc.md` | git mv（保留历史） | 实体文件仅宿主一份，4 业务项目经目录软链自动同步。内容更新：description 与正文 `/dev`→`/cc`、注明「cc = constitution coding」；正文保留 13 行结构与三模式判别。**禁止**逐项目删/建实体文件 |

### 2.2 引用更新（18 文件 46 行，行号已逐条 sed 核实）

> 说明：任务书清单 41 行全部核实存在；兜底扫描**新增 7 行**（表格中以 ⚠ 标注，均属当前状态描述而非修订记录，不更新则验收 1/2 不达标，纳入更新——与编排者 T2「兜底发现默认纳入」同判）。保留行 2 行（修订记录白名单）。实际更新 39 + 7 = 46 行。

**配置域（3 文件 3 行）**

| 文件 | 行号 | 替换前 | 替换后 |
|------|------|--------|--------|
| `.opencode/agents/charter-orchestrator.md` | L14 | `- \`/dev\` 命令的 $ARGUMENTS：任务书文件路径 或 一句话需求。` | `- \`/cc\` 命令的 $ARGUMENTS：任务书文件路径 或 一句话需求。` |
| `harness/agents/charter-orchestrator.md` | L14 | 同上 | 同上 |
| `.opencode/plugin/charter-gate.ts` | L4 | `opencode 入口（/dev，本文件）与 DSH 入口` | `opencode 入口（/cc，本文件）与 DSH 入口` |

**脚本域（2 文件 3 行）**

| 文件 | 行号 | 替换前 | 替换后 |
|------|------|--------|--------|
| `scripts/install_harness.sh` | L109 | `echo "[harness] 完成。在任一业务项目内输入 /dev <任务书路径> 即可启动宪章研发流程。"` | `...输入 /cc <任务书路径>...`（仅此一行，安装逻辑零改动，编排者 T4 已确认） |
| `scripts/run_charter.sh` | L3 | `# run_charter.sh · DSH 下启动宪章编程流程（/dev 等价物，C1）` | `（/cc 等价物，C1）` |
| `scripts/run_charter.sh` | L59 | `# ---------- 输入判别（与 /dev 一致：任务书 / 一句话需求 / 空=列出） ----------` | `（与 /cc 一致：...）` |

**宪章域（1 文件 1 行）**

| 文件 | 行号 | 替换前 | 替换后 |
|------|------|--------|--------|
| `constitution/constitution.md` | L128 | `...在业务项目内使用 \`/dev <任务书路径>\` 命令触发 charter-orchestrator 编排。` | `...使用 \`/cc <任务书路径>\` 命令触发...`（可顺带括注 cc = constitution coding，保持语义不变；**宪法仅此一处改动**） |

**文档域 docs/harness/（6 文件 13 行）**

| 文件 | 行号 | 替换前 | 替换后 |
|------|------|--------|--------|
| `docs/harness/agents-skills.md` | L29 ⚠ | `任务书路径（来自 \`/dev\` 命令 \`$ARGUMENTS\`）` | `（来自 \`/cc\` 命令 \`$ARGUMENTS\`）` |
| `docs/harness/agents-skills.md` | L60 | `\`.opencode/commands/dev.md\`：` | `\`.opencode/commands/cc.md\`：` |
| `docs/harness/agents-skills.md` | L79 | `\| 任务书路径 \| /dev 输入 \| analyst \|` | `\| /cc 输入 \|` |
| `docs/harness/extend.md` | L36 ⚠ | `重启 opencode...验证 \`/dev\` 命令可用...` | `验证 \`/cc\` 命令可用...` |
| `docs/harness/extend.md` | L54 | `/dev opencode_schedule/20260801/20260801任务1xxx/20260801任务1xxx.md` | `/cc opencode_schedule/...` |
| `docs/harness/extend.md` | L67 ⚠ | `中断会话，问题反馈后重跑 \`/dev\`` | `重跑 \`/cc\`` |
| `docs/harness/extend.md` | L105 ⚠ | `> 20260815 任务2 起为**双体系并行**：opencode 入口（/dev）与 DSH 入口长期并行维护，` | `opencode 入口（/cc）与 DSH 入口长期并行维护`（当前状态声明，非修订记录，纳入更新） |
| `docs/harness/extend.md` | L109 | `opencode 入口：\`.opencode/plugin/charter-gate.ts\`（/dev 流程，已薄适配新核心...）` | `（/cc 流程，...）` |
| `docs/harness/README.md` | L29 | `  /dev <任务书> ────────▶ │   （主编排代理...）` | `  /cc <任务书> ────────▶ │   ...`（架构图对齐保持） |
| `docs/harness/README.md` | L53 | `\| 入口命令 \| \`.opencode/commands/dev.md\` \| \`/dev <任务书>\` 触发编排 \|` | `\`.opencode/commands/cc.md\` \| \`/cc <任务书>\` 触发编排 \|` |
| `docs/harness/README.md` | L57 ⚠ | `**双体系并行**（20260815 任务2）：opencode 入口（/dev）与 DSH 入口两个插件共享...` | `opencode 入口（/cc）与 DSH 入口两个插件共享...` |
| `docs/harness/README.md` | L70 | `输入: /dev <任务书路径 或 一句话需求>` | `输入: /cc <任务书路径 或 一句话需求>` |
| `docs/harness/workflow.md` | L10 | 同上 | 同上 |
| `docs/harness/topology.md` | L128 | `重启 opencode 验证 \`/dev\`。` | `重启 opencode 验证 \`/cc\`。` |

**文档域 docs/hetu-hammurabi/（1 文件 3 行）**

| 文件 | 行号 | 替换前 | 替换后 |
|------|------|--------|--------|
| `docs/hetu-hammurabi/dsh-migration.md` | L20 ⚠ | `\| Commands（\`/dev\`） \| DSH profile + 会话入口...` | `\| Commands（\`/cc\`） \| ...`（机制映射表当前状态） |
| `docs/hetu-hammurabi/dsh-migration.md` | L68 | `# 启动一次研发（业务项目目录内，/dev 等价物 C1）` | `（业务项目目录内，/cc 等价物 C1）` |
| `docs/hetu-hammurabi/dsh-migration.md` | L91 | `opencode 入口 \`.opencode/plugin/charter-gate.ts\`（/dev 流程）已升级为薄适配层` | `（/cc 流程）已升级为薄适配层` |

**文档域 docs/hetu-sybil/（2 文件 13 行更新 + 2 行保留）**

| 文件 | 行号 | 替换前 | 替换后 |
|------|------|--------|--------|
| `docs/hetu-sybil/架构与模块设计.md` | L66 | `通过 \`/dev <任务书路径>\` 触发（\`hetu-hammurabi/.opencode/commands/dev.md\`）` | `通过 \`/cc <任务书路径>\` 触发（\`...commands/cc.md\`）` |
| `docs/hetu-sybil/架构与模块设计.md` | L158 | `研发期经 harness \`/dev\` 命令触发开发流程...\`hetu-hammurabi/.opencode/commands/dev.md\`（研发入口）` | `\`/cc\` 命令 + `cc.md`（一行两处） |
| `docs/hetu-sybil/架构与模块设计.md` | L165 | `harness 共享底座（charter-*、\`/dev\`、charter-gate）不变` | `（charter-*、\`/cc\`、charter-gate）不变` |
| `docs/hetu-sybil/架构与模块设计.md` | L428 | `研发期经 \`/dev <任务书>\` 触发开发流程` | `研发期经 \`/cc <任务书>\` 触发开发流程` |
| `docs/hetu-sybil/架构与模块设计.md` | L456 | `与 \`/dev\` 同构；批量 ≠ 多次实例化` | `与 \`/cc\` 同构；...` |
| `docs/hetu-sybil/架构与模块设计.md` | L514 | `由 \`/dev\` 入口（\`.opencode/commands/dev.md\`）触发` | `由 \`/cc\` 入口（\`.opencode/commands/cc.md\`）触发` |
| `docs/hetu-sybil/架构与模块设计.md` | L516 | `harness 共享底座（charter-* 研发流程代理/技能、charter-gate 插件、\`/dev\` 命令）保持...` | `（...、\`/cc\` 命令）保持...` |
| `docs/hetu-sybil/架构与模块设计.md` | L522 | `...（研发期 \`/dev\` 仅用于开发，运行期用私有入口）` | `（研发期 \`/cc\` 仅用于开发，...）` |
| `docs/hetu-sybil/架构与模块设计.md` | L581 | `\| 命令 \| \`/dev\`（hammurabi 共享，研发入口） \| 私有命令 \`/analyze\`...` | `\| 命令 \| \`/cc\`（hammurabi 共享，研发入口） \| ...` |
| `docs/hetu-sybil/架构与模块设计.md` | L671 | `harness 共享底座（charter-* 研发流程代理/技能、charter-gate 插件、\`/dev\` 命令）保持 hammurabi 共享机制不变` | `（...、\`/cc\` 命令）...` |
| `docs/hetu-sybil/架构与模块设计.md` | L694 | `\| 命令/模板 \| \`hetu-hammurabi/.opencode/commands/dev.md\`、\`templates/task_book.md\` \| 入口与模板 \|` | `\`hetu-hammurabi/.opencode/commands/cc.md\`、...`（编排者 T2 已确认纳入） |
| `docs/hetu-sybil/架构与模块设计.md` | L732 | 修订记录「...与 /dev 一任务一流水线同构...」（2026-08-12 历史） | **保留不动**（编排者 T3 已确认） |
| `docs/hetu-sybil/研发计划.md` | L460 | `在 hetu-sybil 项目内使用 \`/dev <任务书路径>\` 命令触发...（\`hetu-hammurabi/.opencode/commands/dev.md\`；宪章第十三条第 1 款）` | `使用 \`/cc <任务书路径>\` 命令触发...（\`...commands/cc.md\`；...）` |
| `docs/hetu-sybil/研发计划.md` | L503 | `运行期分析入口；研发入口 \`/dev\` 不变` | `研发入口 \`/cc\` 不变` |
| `docs/hetu-sybil/研发计划.md` | L633 | `\`.opencode/commands/dev.md\`、\`scripts/install_harness.sh\` \| 全部/任务 11` | `\`.opencode/commands/cc.md\`、...` |
| `docs/hetu-sybil/研发计划.md` | L661 | 修订记录「...与 /dev 同构...」（2026-08-12 历史） | **保留不动**（编排者 T3 已确认） |

**入门文档（4 文件 10 行）**

| 文件 | 行号 | 替换前 | 替换后 |
|------|------|--------|--------|
| `README.md` | L70 | `│   │   └── dev.md  # /dev 入口命令（任务书路径 或 一句话需求）` | `└── cc.md  # /cc 入口命令（cc = constitution coding，任务书路径 或 一句话需求）`（目录树对齐） |
| `README.md` | L119 | `/dev <任务书路径 或 一句话需求>` | `/cc <任务书路径 或 一句话需求>`（附近可加 cc = constitution coding 说明，验收 4） |
| `README.en.md` | L70 | `└── dev.md  # /dev entry (task book path or one-line requirement)` | `└── cc.md  # /cc entry (cc = constitution coding; ...)` |
| `README.en.md` | L121 | `/dev <task book path 或 one-line requirement>` | `/cc <task book path 或 one-line requirement>` |
| `quick_start.md` | L35 | `/dev implement a generic utility: ...` | `/cc implement a generic utility: ...` |
| `quick_start.md` | L47 | `/dev opencode_schedule/20260801/20260801任务1xxx/20260801任务1xxx.md` | `/cc opencode_schedule/...` |
| `quick_start.md` | L70 ⚠ | `\| \`/dev\` unavailable after switching machines \| Re-run step 1 (symlinks use absolute paths) \|` | `\| \`/cc\` unavailable... \|` |
| `快速上手指南.md` | L35 | `/dev 实现一个通用工具方法：...` | `/cc 实现一个通用工具方法：...` |
| `快速上手指南.md` | L47 | `/dev opencode_schedule/...` | `/cc opencode_schedule/...` |
| `快速上手指南.md` | L70 | `\| 换机器后 /dev 不可用 \| 重跑第 1 步安装脚本（软链为绝对路径） \|` | `\| 换机器后 /cc 不可用 \| ... \|`（附近可加 cc 含义说明，验收 4） |

### 2.3 单元测试（1 个新建文件）

| 文件 | 说明 | 类型 |
|------|------|------|
| `unit_test/test_cc_command.py` | 命令更名正确性断言（8 用例，详见第四章） | 新建 |
| `unit_test/test/test_cc_command_result.txt` | 测试结果落盘（目录已存在，有同格式先例） | 生成 |

### 2.4 资产沉淀（执行后核验）

| 文件 | 说明 | 类型 |
|------|------|------|
| `docs/资源地图.md` | 当前无 dev.md 命令引用（已核实，grep 无命中），预计零改动；仅核验 | 核验 |

---

## 三、技术方案要点

### 3.1 A 子步骤：命令更名（git mv + cc.md 内容设计）

```bash
cd /mnt/d/workspace/hetu-altas/hetu-hammurabi
git mv .opencode/commands/dev.md .opencode/commands/cc.md
```

- dev.md 已被 git 跟踪（`git ls-files` 确认），git mv 保留历史；重命名将进入暂存区，与现有 45 个未提交修改互不干扰。
- 4 业务项目经目录软链自动同步（T1），**不做**任何业务项目内操作。
- 验证：`ls -la` 确认软链目标仍指向宿主 commands 目录；5 路径下 `cc.md` 可见、`dev.md` 消失；md5 一致。

**cc.md 新内容（对照现 dev.md 13 行结构，唯一实质改动 = description/正文加入 cc 含义与 /cc 用法，三模式判别与执行要求原样保留）：**

```markdown
---
description: 宪章编程（cc = constitution coding）：输入任务书路径或一句话需求，自动执行完整研发流程
agent: charter-orchestrator
---
启动宪章研发流程（/cc = constitution coding，宪章编程），输入：$ARGUMENTS

## 输入判别（三种模式）
- **任务书路径**：`$ARGUMENTS` 是已存在的 `.md` 文件路径 → 直接作为任务书，从节点 0 开始执行。
- **一句话需求**：`$ARGUMENTS` 非空且不是已有文件路径 → 先执行节点 -1（charter-taskwriter 生成任务书），再按节点 0-7 执行。
- **空输入**：`$ARGUMENTS` 为空 → 扫描当前项目 `opencode_schedule/` 与 `docs/` 目录，列出可用任务书文件请用户选择。

## 执行要求
严格按编排流程执行：任务书生成(按需) → 校验 → 分析 → 编码 → 单元测试(硬门禁) → 评审 → 研发日志 → 资产沉淀 → 钉钉通知，并在任务目录 `opencode_schedule/<YYYYMMDD>/<任务目录>/研发流程状态.md` 固化每个节点状态。
```

要点：
- **frontmatter description**：注明「cc = constitution coding（宪章编程）」，满足验收 4 与单测断言。
- **正文首句**：注明 `/cc` 命令本身与 cc 含义（命令名即 `/cc`，正文需显式出现 `/cc` 与含义说明，单测 `test_host_cc_exists_and_content` 断言）。
- **命令契约不变**（仅名称变化）：`/cc <任务书路径>` → 节点 0；`/cc <一句话需求>` → 节点 -1 先生成任务书；`/cc`（空）→ 扫描 `opencode_schedule/` 与 `docs/` 列出任务书。
- **不得出现「/dev」字样**（单测断言 description 与正文均无遗留）。

### 3.2 B 子步骤：引用更新（替换策略 + 白名单 + 核验）

**替换策略**（逐文件精确替换，禁止全局 sed 无差别替换）：

1. 对 2.2 清单逐文件按行号定位，**优先精确字符串替换**（保持行内其余文本不动）：
   - `/dev <` → `/cc <`（命令调用形态）
   - `` `/dev` ``（反引号包裹的命令名）→ `` `/cc` ``
   - `/dev 输入`、`/dev 命令`、`/dev 等价物`、`（/dev，`、`（/dev）` 等命令名形态 → `/cc` 对应形态
   - `commands/dev.md` → `commands/cc.md`；`dev.md`（目录树/文件名语境）→ `cc.md`
2. **避免误伤设备路径**：`/dev/urandom`、`/dev/null`、`/dev/tcp`、`/dev/std*`、`/dev/shm` 等含 `/dev/` 前缀的行**明确跳过**（上述清单已排除）。
3. **白名单放行**（保留不动）：架构 L732、研发计划 L661（修订记录）；`opencode_schedule/` 全目录。
4. 含「一行两处」的行（架构 L66/L158/L514、README L70 等）需一次性完成行内全部替换，替换后整行检查。
5. `constitution/constitution.md` 仅 L128 一处，改后 `git diff` 该文件应只有 1 个 hunk。

**核验 grep（执行顺序：替换前记录基线 → 替换后零命中）：**

```bash
# 命令引用核验（期望零命中；剩余命中仅允许：白名单修订记录 + 设备路径）
grep -rn --include="*.md" --include="*.ts" --include="*.py" --include="*.sh" --include="*.yaml" \
  -e "/dev <" -e "/dev " -e "/dev\`" -e "（/dev" -e "commands/dev\.md" -e "commands/dev\." -e "dev\.md" \
  /mnt/d/workspace/hetu-altas/hetu-hammurabi \
  --exclude-dir=opencode_schedule --exclude-dir=node_modules --exclude-dir=.git --exclude-dir=unit_test \
  2>/dev/null | grep -v -e "dev\.md~" -e "/dev/urandom" -e "/dev/null" -e "/dev/tcp" -e "/dev/std" -e "/dev/shm" -e "架构与模块设计.md:732" -e "研发计划.md:661" || echo "零命中"

# /cc 到位核验（期望各域均有命中）
grep -rn -e "/cc" --include="*.md" --include="*.ts" --include="*.sh" \
  .opencode/agents/charter-orchestrator.md harness/agents/charter-orchestrator.md \
  .opencode/plugin/charter-gate.ts constitution/constitution.md docs/harness/ \
  docs/hetu-hammurabi/dsh-migration.md docs/hetu-sybil/ README.md README.en.md \
  quick_start.md 快速上手指南.md 2>/dev/null | grep -c "/cc"
```

### 3.3 C 子步骤：测试验证

- 运行新测试（见第四章）+ 全量回归；基线 212 全量 + topology 20 = 232/232（以实测为准）。
- 测试结果落 `unit_test/test/test_cc_command_result.txt`（沿用现有结果文件格式：用例名 + 说明 + ok/FAIL 逐行 + 汇总）。

---

## 四、单元测试计划

**测试文件**：`unit_test/test_cc_command.py`（unittest 风格，参照 `unit_test/test_harness_topology.py` 结构：`sys.path`/路径用 `os.path.dirname(os.path.abspath(__file__))` 相对定位宿主根；`setUp/tearDown` 隔离临时文件）。

**运行命令**：

```bash
/mnt/d/workspace/hetu-altas/venv-hetu/bin/python -m unittest unit_test.test_cc_command -v
# 结果落盘：测试内将结果写至 unit_test/test/test_cc_command_result.txt
# 全量回归：
/mnt/d/workspace/hetu-altas/venv-hetu/bin/python -m unittest discover -s unit_test -p "test_*.py" -v
```

**用例清单（8 用例 = 7 必选 + 1 可选建议实现）**：

| # | 用例 | 类型 | 断言内容 |
|---|------|------|---------|
| 1 | `test_host_cc_exists_and_content` | 正常 | 宿主 `.opencode/commands/cc.md` 存在；内容含「constitution coding」；含 `/cc` 用法（`$ARGUMENTS` 三模式判别）；**不含**遗留 `/dev` 命令引用（description 与正文逐行检查，排除无） |
| 2 | `test_business_projects_cc_consistent` | 正常 | 4 业务项目（hetu-aether/hetu-mercury/hetu-sybil/hetu-thoth）路径下 `.opencode/commands/cc.md` 内容与宿主完全一致（软链同步；比对文件内容） |
| 3 | `test_dev_md_removed` | 正常 | 宿主与 4 业务项目路径下 `.opencode/commands/dev.md` 均不存在 |
| 4 | `test_no_dev_ref_in_config_domain` | 反案例 | 配置域 5 文件（`.opencode/agents/charter-orchestrator.md`、`harness/agents/charter-orchestrator.md`、`.opencode/plugin/charter-gate.ts`、`scripts/install_harness.sh`、`scripts/run_charter.sh`）grep 命令引用零命中（精确模式 `/dev <`、`/dev\``、`（/dev`；`>/dev/null` 设备语义不在断言范围） |
| 5 | `test_no_dev_ref_in_docs_domain` | 反案例 | 文档域（`constitution/constitution.md`、`docs/harness/*.md`、`docs/hetu-hammurabi/*.md`、`docs/hetu-sybil/*.md`、根 `README.md`/`README.en.md`、`quick_start.md`、`快速上手指南.md`）grep「`/dev <`」「`commands/dev.md`」「`/dev\``」「（/dev」零命中；白名单放行：架构 L732、研发计划 L661、设备路径行 |
| 6 | `test_constitution_updated` | 正常 | `constitution/constitution.md` L128 含「/cc <任务书路径」且不含「/dev <任务书路径」；其余行无命令引用变更（可比对全文除 L128 外无 `/dev`/`/cc` 命令引用） |
| 7 | `test_cc_usage_documented` | 正常 | 根 `README.md`、`快速上手指南.md` 含 `/cc` 引用与 cc = constitution coding 含义说明 |
| 8 | `test_detector_positive`（可选，建议实现） | 边界 | 构造含「/dev <」「commands/dev.md」「/dev\`」的临时模拟文件，断言测试自身使用的检测函数能识别（自检测试器有效性，防测试实现漏检） |

**测试实现要点**：检测函数抽为模块级辅助（统一模式：`/dev <`、`commands/dev.md`、`/dev\``、`（/dev`），各用例复用；宿主根定位 `ROOT = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))`；业务项目路径由 ROOT 上一级 + 项目名拼出（`/mnt/d/workspace/hetu-altas/hetu-*`）。

---

## 五、风险与注意事项

| # | 风险/注意 | 应对 |
|---|----------|------|
| 1 | **软链同步**：改宿主后需确认 4 业务项目自动同步 | 验证方式：`ls -la /mnt/d/workspace/hetu-altas/{hetu-aether,hetu-mercury,hetu-sybil,hetu-thoth}/.opencode/commands/` 确认 `commands -> .../hetu-hammurabi/.opencode/commands` 软链仍在且指向正确（软链本体不可被破坏）；5 路径 `cc.md` md5 全同、`dev.md` 不存在（单测覆盖） |
| 2 | **git mv 与未提交工作区共存** | 工作区现有 45 个未提交修改（任务1/2 产物）。只执行 `git mv`（仅暂存重命名），**不 `git add .`、不 `git commit`、不 `git checkout/stash/reset`**；如 git mv 报错（如工作区状态异常）改用 `mv` + 保留文件内容，但优先 git mv |
| 3 | **不得破坏 20260815 任务1/2 产物** | 任务1（harness 硬约束体系优化）、任务2（双体系并行）的未提交修改文件（`.opencode/plugin/charter-gate.ts`、`docs/harness/*` 等）与本次改动文件**重叠**——编辑时只改目标行，用 `git diff <file>` 确认本次改动仅涉及 /dev→/cc 行；不触碰 `harness/core/*`、`harness/gate_rules.yaml`、`docs/hetu-hammurabi/hard-gate-optimize.md` |
| 4 | **grep 核验排除域** | 核验必须排除：`opencode_schedule/`（历史产物）、`unit_test/`（测试文件自身含 /dev 检测模式属正常——但新测试文件内检测模式是字符串字面量，不影响业务核验）、设备路径（`/dev/urandom` 等）、白名单修订记录（架构 L732、研发计划 L661）；`dev.md~` 编辑器备份文件排除 |
| 5 | **设备路径误伤** | `scripts/run_charter.sh` L180 `>/dev/null`、`docs/harness/gates.md` L88、`docs/hetu-hammurabi/hard-gate-optimize.md` L128 的 `/dev/urandom` 等一律不动；替换时以「命令引用上下文」（`/dev <`、/dev\`、/dev 命令、/dev 输入、（/dev））为模式，禁止裸 `/dev` 全局替换 |
| 6 | **宪章权威性** | `constitution/constitution.md` 为顶层约束，仅 L128 一处修订；改后 `git diff` 核查仅 1 hunk；本任务不改动其他宪法条款 |
| 7 | **修订记录保真** | 架构 L732、研发计划 L661 为历史事实记录，保留原文；单测白名单与此一致（T3 已确认）；后续如需全量统一走另行评审 |
| 8 | **opencode 运行时** | 命令文件更名后当前会话可能仍加载旧命令列表，验证以文件系统 + 单测为准；如需运行时验证，重启 opencode 后输入 `/cc` 应触发 charter-orchestrator（属验收外的人工冒烟） |
| 9 | **结果落盘路径** | `unit_test/test/` 目录已存在（有 3 个先例结果文件），直接写 `test_cc_command_result.txt`，无需建目录；注意任务书 3.3 与产出路径均为 `unit_test/test/test_cc_command_result.txt` |

---

## 六、执行顺序（给编码节点）

1. **A**：`git mv` 宿主 dev.md → cc.md → 按 3.1 内容改写 cc.md → 验证 5 路径同步。
2. **B**：按 2.2 清单逐文件（配置域 → 脚本域 → 宪章域 → docs/harness → docs/hetu-hammurabi → docs/hetu-sybil → 入门文档）精确替换 → 3.2 核验 grep 零命中。
3. **C**：新建 `unit_test/test_cc_command.py`（第四章 8 用例）→ 运行新测试 + 全量回归 → 结果落盘。
4. 资产核验：`docs/资源地图.md` 确认零改动；按 charter-assets 流程登记变更说明。
5. 按 charter-logging 流程产出研发日志、研发流程状态、评审报告于任务目录。

---

## 七、资源匹配清单（charter-analysis 第五步）

| 需求点 | 匹配资源(路径+行号) | 类型 | 依据与偏差 |
|--------|--------------------|------|-----------|
| 命令实体 dev.md（970 字节 13 行，被 git 跟踪） | `.opencode/commands/dev.md`（L1-13） | 代码(命令文件) | 实测 970 字节/13 行/`git ls-files` 已跟踪，与任务书描述一致 |
| 软链机制（T1 关键证据） | `scripts/install_harness.sh` L18-19（LINK_ITEMS 含 commands）+ L81-83（`ln -sfn` 目录软链） | 代码(安装脚本) | 实测 4 项目 `.opencode/commands -> 宿主 commands` 软链；dev.md 5 路径同 inode 1407374883587261、md5 全同 92e5323c…；T1「仅改宿主」成立 |
| 引用更新清单（41 行任务书清单） | 18 文件 41 行（见 2.2 表） | 文档 | sed 逐行核实全部存在，行号与任务书一致 |
| 引用更新清单（兜底扫描新增 7 行） | agents-skills L29、extend L36/L67/L105、README(harness) L57、dsh-migration L20、quick_start L70 | 文档 | 全仓宽松模式 grep 发现，任务书清单未列；均属当前状态描述（非修订记录），按 T2 同判纳入更新，否则验收 1/2 不达标 |
| 不动域核实 | gates.md L88、hard-gate-optimize.md L128（/dev/urandom）、run_charter.sh L180（>/dev/null）、架构 L732、研发计划 L661 | 文档/代码 | 均确认为设备路径或修订记录，保留不动 |
| 测试参考结构 | `unit_test/test_harness_topology.py`（364 行，20 用例，结果落 `unit_test/test/test_harness_topology_result.txt`） | 代码(参考) | 新测试文件沿用其 unittest 风格与相对定位方式；结果目录与格式有先例 |
| 回归基线 | 任务2 研发流程状态.md：全量 212/212 + topology 20/20 = 232/232 | 记录 | 落闸基线以实测为准 |
| 测试运行环境 | `/mnt/d/workspace/hetu-altas/venv-hetu/bin/python`（Python 3.12.3） | 环境 | 实测存在；`<VENV_BIN>` 解析正确 |
| 资源地图 | `docs/资源地图.md`（167 行） | 文档 | 全文无 dev.md 命令引用（grep 无命中），资产核验预计零改动 |
| 未找到 | — | — | 无（本任务为 meta 任务，资源即改造对象本身，全部匹配） |
