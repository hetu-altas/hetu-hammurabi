# 任务3研发日志 · dev命令更名cc

## 一、任务概述

本任务为**机制自优化任务（meta task）**：改造对象即 opencode 入口命令 `/dev` 本身。一句话需求：「把 /dev 命令改成 /cc，意思就是 constitution coding」。

opencode 命令名 = `.opencode/commands/<文件名>`，改命令即改文件名。原命令文件为宿主 `.opencode/commands/dev.md`（970 字节，13 行，已被 git 跟踪），命令引用散布于配置域、脚本域、宪章域、文档域（docs/harness/、docs/hetu-hammurabi/、docs/hetu-sybil/）与入门文档。

完成以下改造：

1. **命令更名**（核心产出）：宿主 `dev.md` → `cc.md`（git mv 保留历史），内容更新——frontmatter description 与正文注明「cc = constitution coding（宪章编程）」，正文全文无 `/dev` 残留，三模式判别与执行要求语义原样保留；
2. **引用更新**：18 文件 46 处引用更新（`/dev` → `/cc`、`dev.md` → `cc.md`）——任务书清单 41 行中 39 行更新 + 2 处修订记录白名单保留（架构 L732、研发计划 L661），兜底扫描新增 7 行全部纳入；
3. **软链自动同步核实**：4 个业务项目（hetu-aether/hetu-mercury/hetu-sybil/hetu-thoth）的 `.opencode/commands` 均为目录级软链指向宿主，**只需改宿主实体文件**，4 项目自动同步（更名后 5 路径 cc.md md5 全同 `34d098e8…`）；
4. **测试与验收**：新增 `unit_test/test_cc_command.py` 8 用例（≥6 达标）+ 全量回归不回归，落闸 248/248。

**不动边界全程守住**：`constitution/constitution.md` 仅 L128（第十三条第 1 款）一处命令引用更新，其余零改动；`opencode_schedule/` 历史产物不动；`harness/core/*`、`harness/gate_rules.yaml`、`harness/plugins/*`、`harness/workflow.yaml`、`scripts/attach_native_panel.sh`、`templates/task_book.md` 零改动；设备路径（`/dev/urandom`、`/dev/null`、`/dev/tcp`、`/dev/std*`、`/dev/shm`）一律不动；修订记录行（架构 L732、研发计划 L661）白名单保留原文；git 仅 `git mv` 一个操作未 commit，20260815 任务1/2 的 45 个未提交产物原样保留。

## 二、创建/修改文件清单

### 2.1 命令更名（1 个文件操作，4 项目经软链自动同步）

| 文件 | 说明 | 类型 |
|------|------|------|
| `.opencode/commands/dev.md` → `cc.md` | **核心产出**：git mv 保留历史；内容更新——description「宪章编程（cc = constitution coding）：输入任务书路径或一句话需求，自动执行完整研发流程」、正文首句「启动宪章研发流程（/cc = constitution coding，宪章编程）」；13 行结构与三模式判别（任务书路径/一句话需求/空输入）语义原样保留；全文无 `/dev`；4 业务项目经目录软链自动同步（**禁止**逐项目删/建实体文件） | 修改(重命名) |

### 2.2 引用更新（18 文件 46 处，行号已逐条核实）

> 口径：任务书清单 41 行中 39 行更新 + 2 处修订记录白名单保留（架构 L732、研发计划 L661）；兜底扫描新增 7 行（agents-skills L29、extend L36/L67/L105、README(harness) L57、dsh-migration L20、quick_start L70，均属当前状态描述非修订记录，编排者 T2 同判纳入）→ 实际更新 **46 处**。

**配置域（3 文件 3 处）**

| 文件 | 替换处数 | 说明 |
|------|---------|------|
| `.opencode/agents/charter-orchestrator.md` | 1（L14） | 「`/dev` 命令的 $ARGUMENTS」→「`/cc` 命令的 $ARGUMENTS」 |
| `harness/agents/charter-orchestrator.md` | 1（L14） | 同上（双代理文件 L14 一致） |
| `.opencode/plugin/charter-gate.ts` | 1（L4） | 注释「opencode 入口（/dev，本文件）与 DSH 入口」→「（/cc，本文件）」 |

**脚本域（2 文件 3 处）**

| 文件 | 替换处数 | 说明 |
|------|---------|------|
| `scripts/install_harness.sh` | 1（L109） | echo 提示「输入 /dev <任务书路径>」→「/cc <任务书路径>」（安装逻辑 LINK_ITEMS 零改动，编排者 T4 已确认） |
| `scripts/run_charter.sh` | 2（L3/L59） | 注释「（/dev 等价物，C1）」→「/cc」；「（与 /dev 一致：任务书 / 一句话需求 / 空=列出）」→「/cc」（L180 `>/dev/null` 设备路径保留） |

**宪章域（1 文件 1 处）**

| 文件 | 替换处数 | 说明 |
|------|---------|------|
| `constitution/constitution.md` | 1（L128） | 第十三条第 1 款「使用 `/dev <任务书路径>` 命令」→「`/cc <任务书路径>`」+ 括注 cc = constitution coding；**宪法仅此一处改动**（git diff 净改动 1 hunk） |

**文档域 docs/harness/（5 文件 13 处）**

| 文件 | 替换处数 | 说明 |
|------|---------|------|
| `docs/harness/agents-skills.md` | 3（L29⚠/L60/L79） | L29 任务书路径来源「/dev 命令」；L60 `.opencode/commands/dev.md`→`cc.md`；L79「/dev 输入」→「/cc 输入」 |
| `docs/harness/extend.md` | 5（L36⚠/L54/L67⚠/L105⚠/L109） | 重启验证 /dev、示例调用、中断重跑 /dev、双体系并行声明 opencode 入口（/dev）、薄适配注释（/dev 流程）→ 全部 /cc |
| `docs/harness/README.md` | 4（L29/L53/L57⚠/L70） | 架构图「/dev <任务书>」、入口命令表（dev.md → cc.md）、双体系并行声明、输入示例 → 全部 /cc |
| `docs/harness/workflow.md` | 1（L10） | 「输入: /dev <任务书路径 或 一句话需求>」→「/cc」 |
| `docs/harness/topology.md` | 1（L128） | FAQ「重启 opencode 验证 `/dev`」→「/cc」 |

**文档域 docs/hetu-hammurabi/（1 文件 3 处）**

| 文件 | 替换处数 | 说明 |
|------|---------|------|
| `docs/hetu-hammurabi/dsh-migration.md` | 3（L20⚠/L68/L91） | 机制映射表 Commands（/dev）、「/dev 等价物 C1」、「（/dev 流程）已升级为薄适配层」→ 全部 /cc；**L73「/CC」大写引用因大小写敏感保留未误动**（抽查确认） |

**文档域 docs/hetu-sybil/（2 文件 14 处更新 + 2 处白名单保留）**

| 文件 | 替换处数 | 说明 |
|------|---------|------|
| `docs/hetu-sybil/架构与模块设计.md` | 11（L66/L158/L165/L428/L456/L514/L516/L522/L581/L671/L694） | `/dev <任务书>` 触发、研发入口 dev.md→cc.md、harness 共享底座（/dev→/cc）、「与 /cc 同构」（语义完整）、资源索引表（L694，编排者 T2 确认纳入）等 11 行；**L732 修订记录（2026-08-12 历史）白名单保留** |
| `docs/hetu-sybil/研发计划.md` | 3（L460/L503/L633） | 「使用 /dev <任务书路径> 命令触发…commands/dev.md」→ /cc + cc.md；「研发入口 /dev 不变」→ /cc；任务矩阵 dev.md → cc.md；**L661 修订记录（2026-08-12 历史）白名单保留** |

**入门文档（4 文件 10 处）**

| 文件 | 替换处数 | 说明 |
|------|---------|------|
| `README.md` | 2（L70/L119） | 目录树「└── dev.md  # /dev 入口命令」→「cc.md  # /cc 入口命令（cc = constitution coding…）」；用法示例 /dev → /cc |
| `README.en.md` | 2（L70/L121） | 目录树与用法示例同步（cc = constitution coding） |
| `quick_start.md` | 3（L35/L47/L70⚠） | 示例 /dev → /cc（L70 FAQ「/dev unavailable」→ /cc） |
| `快速上手指南.md` | 3（L35/L47/L70） | 示例 /dev → /cc（L70 FAQ「换机器后 /cc 不可用」） |

### 2.3 单元测试（新建）

| 文件 | 说明 | 类型 |
|------|------|------|
| `unit_test/test_cc_command.py` | 命令更名正确性断言（8 用例：正常 4 + 反案例 2 + 边界 2，详见第四章） | 新建 |
| `unit_test/test/test_cc_command_result.txt` | 测试结果落盘（Ran 8 tests · OK，8/8 全过） | 生成 |
| `unit_test/test/test_harness_dsh_result.txt` | 全量回归结果更新（Ran 220 tests · OK = 212 基线 + 8 新增） | 修改 |

## 三、核心设计

### 1. cc.md 结构与语义（对照 dev.md 13 行，唯一实质改动 = 名称与含义）

```markdown
---
description: 宪章编程（cc = constitution coding）：输入任务书路径或一句话需求，自动执行完整研发流程
agent: charter-orchestrator
---
启动宪章研发流程（/cc = constitution coding，宪章编程），输入：$ARGUMENTS

## 输入判别（三种模式）
- **任务书路径**：`$ARGUMENTS` 是已存在的 `.md` 文件路径 → 直接作为任务书，从节点 0 开始执行。
- **一句话需求**：`$ARGUMENTS` 非空且不是已有文件路径 → 先执行节点 -1（charter-taskwriter 生成任务书），再按节点 0-7 执行。
- **空输入**：`$ARGUMENTS` 为空 → 扫描当前项目 `opencode_schedule/` 与 `docs/` 目录，列出可用任务书文件请用户选择。

## 执行要求
严格按编排流程执行：任务书生成(按需) → 校验 → 分析 → 编码 → 单元测试(硬门禁) → 评审 → 研发日志 → 资产沉淀 → 钉钉通知，并在任务目录 .../研发流程状态.md 固化每个节点状态。
```

- **命令契约不变（仅名称变化）**：`/cc <任务书路径>` → charter-orchestrator 节点 0；`/cc <一句话需求>` → 节点 -1 先生成任务书；`/cc`（空）→ 扫描 `opencode_schedule/` 与 `docs/` 列出任务书；
- **含义说明三处落位**：frontmatter description、正文首句（`/cc = constitution coding`）、宪章 L128 括注——满足验收 4 与单测断言；
- **不得出现「/dev」字样**：单测逐行断言 description 与正文均无遗留。

### 2. 软链自动同步机制核实（inode / md5 实测，推翻「4 项目逐个删/建」原方案）

- **机制来源**：`scripts/install_harness.sh` L18-19 `LINK_ITEMS=(agents commands skills plugin ...)` + L81-83 `ln -sfn`——4 个业务项目的 `.opencode/commands` 均为**目录级软链**指向宿主 `<HARNESS_DIR>/.opencode/commands`；
- **更名前核实**：5 路径下 `dev.md` md5 全同 `92e5323c…`、**inode 全同 `1407374883587261`**（同一实体文件经软链可见）；
- **结论（T1 决策）**：只需改宿主实体文件，4 项目自动同步生效；逐个删/建实体文件反而破坏软链共享机制、造成后续同步脱节（如未来任务 11 目录混合化，另行评估）；
- **更名后核实**：4 项目 `commands -> 宿主 commands` 软链本体完好（ls -la 确认，指向不变）、inode 未变；5 路径 `cc.md` md5 全同 `34d098e8…`；5 路径 `dev.md` 均不存在；git mv 仅移动宿主实体文件，不触碰业务项目任何文件。

### 3. 精确替换策略与设备路径防误伤

- **逐文件精确替换**（禁止全局 sed 无差别替换）：按清单逐文件行号定位，优先精确字符串替换，保持行内其余文本不动——`/dev <` → `/cc <`（命令调用形态）、`` `/dev` ``（反引号包裹）→ `` `/cc` ``、`/dev 输入`/`/dev 命令`/`/dev 等价物`/`/dev 流程`/`（/dev，`/`（/dev）` 等命令名形态 → `/cc` 对应形态、`commands/dev.md` → `commands/cc.md`；
- **设备路径防误伤**：`/dev/urandom`、`/dev/null`、`/dev/tcp`、`/dev/std*`、`/dev/shm` 等含 `/dev/` 前缀的行明确跳过——`docs/harness/gates.md` L88、`docs/hetu-hammurabi/hard-gate-optimize.md` L128 的 `/dev/urandom`、`scripts/run_charter.sh` L180 的 `>/dev/null` 实测保留；
- **一行两处行**（架构 L66/L158/L514、README L70 等）一次性完成行内全部替换，替换后整行检查（架构 L456「与 /cc 同构；批量 ≠ 多次实例化」替换后语义完整）；
- **大小写敏感**：dsh-migration L73「GUI 钩子（/CC 命令，constitution coding…）」大写 `/CC` 未被替换（抽查确认未误动，观察项见第五章）；
- **核验 grep 零命中**：模式 `/dev <`、`/dev `、`/dev``、`（/dev`、`commands/dev.md`、`dev.md`，排除 `opencode_schedule/`、`unit_test/`、`node_modules/`、`.git/`、`runlog/`、白名单（架构 L732、研发计划 L661）、设备路径后**零命中**（grep 退出码非 0）；`/cc` 到位经 grep -rc 逐文件确认（配置域/脚本域/宪章 L128/docs-harness/dsh-migration/docs-sybil/README 双版/quick_start/快速上手指南）。

### 4. 白名单决策（修订记录保真）

| 决策项 | 结论 | 依据 |
|--------|------|------|
| 架构 L732、研发计划 L661（修订记录） | **保留原文不动**（编排者 T3 确认） | 2026-08-12 历史事实记录，与 opencode_schedule/ 历史产物同理保真历史；单测 REVISION_WHITELIST 放行（实测两行仍在） |
| 架构 L694（资源索引表） | 纳入更新（T2 确认） | 兜底扫描发现，属当前资源索引非修订记录，不更新则验收 1/2 不达标 |
| 软链同步（T1） | 采纳「仅改宿主」 | inode/md5 实测同一实体文件 |
| install_harness.sh（T4） | 仅 L109 echo 提示更新，LINK_ITEMS 逻辑不动 | cc.md 与 dev.md 同目录，软链自动覆盖 |

### 5. git mv 保留历史与工作区共存

- `dev.md` 已被 git 跟踪（`git ls-files` 确认），`git mv` 保留完整历史；实测 `git status` 暂存区仅 **`RM .opencode/commands/dev.md -> .opencode/commands/cc.md`** 一个操作；
- 与 20260815 任务1/2 的 45 个未提交产物互不干扰：不 `git add .`、不 commit、不 checkout/stash/reset（HEAD 仍为 d5a8e05，git 无新 commit）；
- ⚠️ 评审提示：git mv 重命名已入暂存区、内容改动在未暂存区——后续提交时需先 `git add .opencode/commands/cc.md` 方可将内容变更一并提交（计划内行为）。

## 四、测试结果

**落闸口径：248/248 全绿，GATE:PASS**（`.gate.json`：test_passed=true、total=passed=248、schema_version=2、run_id 契约一致、gate_token 校验通过、written_by=charter-orchestrator）

- **落闸构成**：全量回归 **220/220**（`unit_test/test/test_harness_dsh_result.txt`，Ran 220 tests · OK；= 212 基线 + 8 新增，discover 含 test_cc_command.py）+ 存量拓扑 **20/20**（`unit_test/test/test_harness_topology_result.txt`，八格式与原生格式交叉核对一致）+ cc 命令单测独立复跑 **8/8**（`unit_test/test/test_cc_command_result.txt`）= **248/248**；基线 212/212 不回归
- **新增 8 用例**（≥6 达标，unittest 风格参照 test_harness_topology.py；检测函数抽为模块级辅助 `has_dev_ref`/`scan_dev_ref` 供复用）：

| 用例 | 类型 | 覆盖点 |
|------|------|--------|
| `test_host_cc_exists_and_content` | 正常 | 宿主 `.opencode/commands/cc.md` 存在；含「constitution coding」含义；含 `/cc` 用法（$ARGUMENTS 三模式）；**不含**遗留 `/dev` 引用（description 与正文逐行） |
| `test_business_projects_cc_consistent` | 正常 | 4 业务项目 `.opencode/commands/cc.md` 内容与宿主完全一致（软链同步，内容比对） |
| `test_dev_md_removed` | 正常 | 宿主与 4 业务项目路径下 `dev.md` 均不存在 |
| `test_no_dev_ref_in_config_domain` | 反案例 | 配置域 5 文件（`.opencode/agents/charter-orchestrator.md`、`harness/agents/charter-orchestrator.md`、`.opencode/plugin/charter-gate.ts`、`scripts/install_harness.sh`、`scripts/run_charter.sh`）grep「`/dev <`」「`/dev\``」「（/dev」零命中（`>/dev/null` 设备语义不在断言范围） |
| `test_no_dev_ref_in_docs_domain` | 反案例 | 文档域 16 文件 grep 零命中；白名单放行：架构 L732、研发计划 L661、设备路径行 |
| `test_constitution_updated` | 正常 | `constitution/constitution.md` L128 含「/cc <任务书路径」与 constitution coding，不含「/dev <任务书路径」；全文其余行无命令引用变更 |
| `test_cc_usage_documented` | 正常 | 根 `README.md`、`快速上手指南.md` 含 `/cc` 引用与「constitution coding」含义说明 |
| `test_detector_positive` | 边界 | 构造含「/dev <」「commands/dev.md」「/dev`」「（/dev」的模拟文件，检测函数 4 模式均能识别；清洁文本（`>/dev/null`、`/dev/urandom`、修订记录语境）不误报（自检测试器有效性） |

- **既有用例零改动**：全量 220 中其余 212 用例内容完整未动（基线 212/212 不回归）；
- **评审**：**REVIEW:APPROVE**（2 项低严重度观察项，均不构成 REVISE；验收标准 1-4 逐条对照全部 ✅：命令就位/引用清零/测试门禁/含义说明）。

### grep 核验要点（实测）

- 命令引用核验（排除 opencode_schedule、unit_test、node_modules、.git、runlog、白名单修订记录、设备路径）：`/dev <`、`/dev `、`/dev\``、`（/dev`、`commands/dev.md`、`dev.md` **零命中**；
- `/cc` 到位核验：配置域 3 文件 + 宪章 L128 + docs/harness 5 文件（grep -rc：agents-skills 3、extend 5、README 4、workflow 1、topology 1）+ dsh-migration 3 + docs-sybil（架构 12、研发计划 3）+ 入门文档（README 2、README.en 2、quick_start 3、快速上手指南 4）逐文件命中；
- 设备路径与白名单复核：gates.md L88、hard-gate-optimize.md L128 `/dev/urandom`、run_charter.sh L180 `>/dev/null`、架构 L732、研发计划 L661 原样保留（grep 实测仍在）；
- 软链复核：4 项目 commands 软链完好、5 路径 cc.md md5 全同 `34d098e8…`、dev.md 消失。

## 五、遗留问题与后续事项

1. **评审遗留观察项 2 条（低，登记不阻断）**：
   - ① `unit_test/test_cc_command.py` L38-43（DEV_REF_PATTERNS）：检测模式仅覆盖 4 种形态（`/dev <`、`commands/dev.md`、`/dev\``、`（/dev`），未覆盖「/dev 命令」「/dev 输入」「/dev 等价物」「/dev 流程」等形态；当前仓库宽松 grep（含 `/dev ` 模式）已零命中无实际残留，但未来新增此类引用时单测会漏检——建议后续补充模式或注释说明检测模式与替换形态的对应关系及局限；
   - ② `unit_test/test_cc_command.py` L176 docstring：`test_no_dev_ref_in_config_domain` 的 docstring 仅列 3 个模式，与 `scan_dev_ref` 实际使用的 4 个模式（含 `commands/dev.md`）不一致——建议同步 docstring，避免后续维护误导。
2. **重启 opencode 后 /cc 生效验证**：命令文件为配置启动时加载、不热更新，当前会话可能仍显示旧命令列表；下次重启 opencode 后输入 `/cc` 应触发 charter-orchestrator（人工冒烟），并顺带核查 `.opencode/plugin/charter-gate.ts` 插件加载日志（任务2 遗留项一并验证）。
3. **dsh-migration L73「/CC」大写引用观察**：L73「GUI 钩子（/CC 命令，constitution coding…）」因大小写敏感未被替换（本次按「不误动」处理，抽查确认）；若后续要求命令引用大小写全量统一，需另行确认（`/cc` 小写为命令规范形态，大写属该文档既有表述）。
4. **git 提交提示**（评审遗留 3，计划内行为）：git mv 重命名已入暂存区（`RM dev.md -> cc.md`）、cc.md 内容改动在未暂存区——后续提交时需先 `git add .opencode/commands/cc.md` 方可将内容变更一并提交；git 当前无新 commit（HEAD 仍为 d5a8e05），45 个任务1/2 未提交产物原样保留。
