# 研发流程状态 · 20260814任务2 宪章看板DSH原生面板

> 由 charter-orchestrator 维护，每完成一个节点追加一条记录。

| 时间 | 节点 | 状态 | 说明 |
|------|------|------|------|
| 2026-08-14 | -1 任务书生成 | 通过 | 任务书含：代理方案 4 项局限（L1-L4）、DSH client-plugin 双面机制（侦察结论）、9 条验收标准、2 阶段拆分 |
| 2026-08-14 | 0 校验 | 通过 | 任务书/宪章可访问；任务目录创建 |
| 2026-08-14 | 1 分析 | 通过 | 侦察：client-plugin 双面结构（node 半区 cordis + browser 半区 __ModuleLoader__）、dsh.client 元数据、SlotMap 插槽（sidebar.footer.action / shell.overlay 为 additive list 挂载点）、loader 包名解析规则 |
| 2026-08-14 | 2 编码 | 通过 | 交付：client.js（browser 半区：按钮+全屏浮层+5 视图+主题+降级态）、lib/index.js（node 半区）、package.json（双面元数据）、dashboard-proxy.ts 升级（新增 /api/hetu-dashboard 数据通道 + 降级 JSON）、单测 7 个、文档 2 篇 |
| 2026-08-14 | 3 单元测试 | 通过 | GATE:PASS 7/7（node:test：stripPrefix 3 + 转发集成 1 + 数据契约 3）；契约与 stats.py 输出对齐；Python 侧 106 用例回归不受影响；seal-gate 落闸 .gate.json v2 |
| 2026-08-14 | 4 代码评审 | 通过 | REVIEW:APPROVE（GUI 实测验收：manifest 注入 + client.js 200 + 数据通道真实数据 + 侧边栏按钮可见）；插槽选择 additive 不替换现有 UI |
| 2026-08-14 | 5 研发日志 | 通过 | 任务2研发日志（含 4 轮踩坑完整记录与最终验收） |
| 2026-08-14 | 6 资产沉淀 | 通过 | 新增 dsh-client-plugin.md；更新 dashboard.md（原生面板章节）+ 资源地图登记 |
| 2026-08-14 | 7 通知 | 通过 | 通过唯一出口发送钉钉成功（errcode:0），标题「20260814任务2 宪章看板DSH原生面板 完成」 |

## 编排决策

| # | 决策点 | 决策 | 理由 |
|---|--------|------|------|
| 1 | 构建链 vs 手写 client.js | github 不可达（克隆失败）→ 手写 UMD client.js（仿官方产物形态） | 环境受限下的务实降级；源码等价物后续补 |
| 2 | 数据通道 | dashboard-proxy.ts 增加 /api/hetu-dashboard 前缀路由（复用已验证的 webServer.register 机制） | 避免重复实现转发；node 半区保持最小 |
| 3 | 插槽挂载点 | sidebar.footer.action（按钮）+ shell.overlay（浮层） | 均为 list 型 additive 插槽，不替换现有 UI（sidebar/conversation 等 single 插槽会替换占用者，禁用） |
| 4 | 包注册位置 | **dsh 安装目录 node_modules**（loader import(name) 的解析上下文） | 踩坑结论：loader 不解析 profile node_modules 的包名；绝对路径 .ts 与安装目录包名两种方式可用 |
| 5 | API 路径 | client.js fetch /api/hetu-dashboard/**api**/*（剥前缀后命中 8790 的 /api/*） | 转发链的路径契约 |

## 流程总结

- 改动文件：新建 8（client.js/lib/index.js/package.json/test 2 个/文档 2 篇）+ 修改 2（dashboard-proxy.ts、资源地图）
- 测试通过：7/7（node 半区 + 数据契约）
- 验收达成：**原生面板挂接成功**（GUI 实测：manifest/client.js/数据通道/侧边栏按钮/全屏浮层）；一键挂接脚本固化
- 遗留事项：① 官方构建链/HMR 待网络可达后接入；② 可构建 TSX 源码副本（src/client/）待补全；③ attach_native_panel.sh 需在部署机执行（写 $DSH_HOME）
