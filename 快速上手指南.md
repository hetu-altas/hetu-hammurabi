# 快速上手指南

> 5 分钟跑通第一个「宪章编程」任务。完整说明见 [docs/harness/](docs/harness/README.md)。

## 0. 前置条件

| 依赖 | 要求 |
|------|------|
| opencode | ≥ 1.16（当前唯一适配的 harness，后续将适配更多国产 harness） |
| 模型 | 任意已配置 provider（推荐 DeepSeek V4 Flash 等国产模型） |
| Python | 如需跑单元测试：`unittest` + 项目 venv |
| 目录布局 | `hetu-hammurabi` 与业务项目（`hetu-xxx`）**平级部署**于同一父目录 |

## 1. 安装 harness（30 秒）

```bash
bash hetu-hammurabi/scripts/install_harness.sh
```

验证安装：

```bash
opencode agent list        # 应看到 charter-orchestrator + 8 个节点子代理
opencode debug skill       # 应看到 charter-* 技能
opencode debug config      # plugin 列表应含 charter-gate.ts
```

## 2. 启动第一个任务（任选一种输入）

### 方式 A：一句话需求（推荐首次体验）

在业务项目目录启动 opencode 后：

```
/dev 实现一个通用工具方法：对列表按日期字段排序并去重，输出到指定文件
```

系统会自动：生成任务书 → 分析 → 编码 → 单元测试(硬门禁) → 评审 → 研发日志 → 资产沉淀 → 钉钉通知。

### 方式 B：任务书

1. 复制 `hetu-hammurabi/templates/task_book.md` 模板并填写
2. 放入业务项目任务目录：`opencode_schedule/<YYYYMMDD>/<YYYYMMDD>任务N<名称>/`
3. 执行：

```
/dev opencode_schedule/20260801/20260801任务1xxx/20260801任务1xxx.md
```

## 3. 查看产物

所有中间产物归档在任务目录：

```
opencode_schedule/<YYYYMMDD>/<YYYYMMDD>任务N<名称>/
├── 任务书           # 输入（或自动生成）
├── 实施计划.md      # 分析节点
├── .gate.json       # 硬门禁状态（测试通过后生成）
├── 评审报告.md      # 评审节点
├── 任务N研发日志.md # 日志节点
└── 研发流程状态.md  # 全流程节点状态
```

## 4. 常见问题

| 问题 | 处理 |
|------|------|
| 配置修改后不生效 | **退出并重启 opencode**（配置启动时加载） |
| 报"测试门禁未通过" | 这是预期行为：测试节点跑完并生成 `.gate.json` 后自动放行；人为写日志/发通知会被硬拦截 |
| 换机器后 /dev 不可用 | 重跑第 1 步安装脚本（软链为绝对路径） |
| 提示路径不存在 | 确认 `hetu-hammurabi` 与业务项目在同一父目录下平级部署 |
| 路径与我的环境不符 | 仓库使用相对路径约定（`../venv-hetu/`、`../logs/`、`../hetu-aether/`），按实际布局替换，见 README「开源适配」小节 |
| 想跳过某个节点 | 不支持跳过——流程按宪章固化，可调整任务粒度而非节点 |

## 5. 下一步

- **完整体系文档**：[docs/harness/](docs/harness/README.md)（总览 / 工作流 / 代理与技能 / 门禁 / 资产 / 扩展维护）
- **宪章规范**：`constitution/`（顶层 13 章 + 7 部子规范：编码 / 单测 / 日志 / 项目 / TDengine / Milvus / 任务拆分）
- **核心理念**：[《宪章编程宣言》](宪章编程宣言.md)
- **任务粒度评估**：任务书编写前按 `constitution/task_split/task_split.md` 评估，超限先拆分

---

项目基于 MIT 协议开源，商用无限制。问题与建议请联系：hetu_altas@163.com
