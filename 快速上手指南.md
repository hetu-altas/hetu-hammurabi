# 快速上手指南

> 5 分钟跑通第一个「宪章编程」任务。完整说明见 [docs/harness/](docs/harness/README.md) 与
> [docs/hetu-hammurabi/dsh-client-plugin.md](docs/hetu-hammurabi/dsh-client-plugin.md)。

## 0. 前置条件

| 依赖 | 要求 |
|------|------|
| DSH | `npx @deepseek-ai/dsh web`（DeepSeek Harness，当前主推执行载体） |
| opencode | ≥ 1.16（兼容路径，旧流程仍可用） |
| 模型 | 任意已配置 provider（推荐 DeepSeek / Qwen / GLM / Kimi 等国产模型） |
| Python | 看板数据服务需要 `venv-hetu`（fastapi/uvicorn） |
| 目录布局 | `hetu-hammurabi` 与业务项目（`hetu-xxx`）**平级部署**于同一父目录 |

## 1. 安装（两条）

```bash
# ① Python 数据服务 + 宪章 + core（仓库即服务，git clone 即用）

# ② DSH 一体化插件（/cc 命令 + 硬门禁 + 看板面板 + 状态栏 + 看板自启）
cd hetu-hammurabi
npx @deepseek-ai/dsh plugin --profile web add ./plugins/constitution-coding
```

> opencode 兼容路径（可选）：`bash scripts/install_harness.sh` 安装旧 harness，业务项目内 `/dev` 可用。

## 2. 启动 DSH（看板会自动跟着起来）

```bash
# 在业务项目或工作区根目录启动
npx @deepseek-ai/dsh web --port 3090
```

- DSH 启动约 2 秒后，看板数据服务（8790）被插件钩子**自动拉起**（无需手动启动）
- 若 3080 被 Windows 应用（如 attu）占用，用 `--port` 换端口即可（见 dsh-client-plugin.md 端口冲突章节）

## 3. 启动第一个任务（任选一种输入）

### 方式 A：GUI 里用 /cc 命令（推荐）

在 DSH web GUI 输入框直接输入：

```
/cc 实现一个通用工具方法：对列表按日期字段排序并去重，输出到指定文件
```

或指定任务书：

```
/cc opencode_schedule/20260801/20260801任务1xxx/20260801任务1xxx.md
```

系统自动执行：任务书生成（按需）→ 分析 → 编码 → 单元测试（硬门禁）→ 评审 → 研发日志 → 资产沉淀 → 钉钉通知。
执行期间**右侧流程状态栏**实时显示节点 -1~7 进展，硬门禁（危险命令/通知出口/日志门禁）由机器拦截。

### 方式 B：任务书

1. 复制 `templates/task_book.md` 模板并填写
2. 放入业务项目任务目录：`opencode_schedule/<YYYYMMDD>/<YYYYMMDD>任务N<名称>/`
3. 执行方式 A 的 `/cc <任务书路径>`

### 方式 C：CLI（headless）

```bash
bash <HARNESS_DIR>/scripts/run_charter.sh <任务书路径 或 一句话需求>   # --dry-run 预览提示词
```

## 4. 查看产物

所有中间产物归档在任务目录：

```
opencode_schedule/<YYYYMMDD>/<YYYYMMDD>任务N<名称>/
├── 任务书           # 输入（或自动生成）
├── 实施计划.md      # 分析节点
├── .gate.json       # 硬门禁状态（测试通过后落闸，v2 签名）
├── 评审报告.md      # 评审节点
├── 任务N研发日志.md # 日志节点
└── 研发流程状态.md  # 全流程节点状态
```

看板（GUI 侧边栏「📊 宪章看板」或独立访问 `http://127.0.0.1:8790/`）：
总览（任务数/节点完成/成功率/门禁拦截率）、**引擎性能对比（DSH vs opencode）**、节点统计、门禁拦截记录、任务列表与详情——统计覆盖全部 hetu-* 项目。

## 5. 常见问题

| 问题 | 处理 |
|------|------|
<<<<<<< Updated upstream
| 配置修改后不生效 | **退出并重启 opencode**（配置启动时加载） |
| 报"测试门禁未通过" | 这是预期行为：测试节点跑完并生成 `.gate.json` 后自动放行；人为写日志/发通知会被硬拦截 |
| 换机器后 /dev 不可用 | 重跑第 1 步安装脚本（软链为绝对路径） |
| 提示路径不存在 | 确认 `hetu-hammurabi` 与业务项目在同一父目录下平级部署 |
=======
| `/cc` 命令没出现 | 确认已 `npx @deepseek-ai/dsh plugin --profile web add ./plugins/constitution-coding` 且重启过 dsh web（配置启动时加载） |
| 命令必须小写 | `/CC` 大写会导致 dsh web 启动失败（DSH 命令名规范） |
| 看板没自动起来 | 检查 `logs/hetu-altas/dashboard.log`；手动 `bash scripts/start_dashboard.sh` 兜底 |
| attu/其他应用占用端口 | `npx @deepseek-ai/dsh web --port <新端口>` 换端口 |
| 任务归属错项目 | /cc 按任务内容归属业务项目；从工作区根运行时尤其注意 |
>>>>>>> Stashed changes
| 想跳过某个节点 | 不支持跳过——流程按宪章固化，可调整任务粒度而非节点 |

## 6. 下一步

- **DSH 插件开发指南**：[docs/hetu-hammurabi/dsh-client-plugin.md](docs/hetu-hammurabi/dsh-client-plugin.md)（双面半区/打包/挂载/踩坑）
- **DSH 官方文档离线镜像**：[docs/dsh-docs/](docs/dsh-docs/README.md)
- **宪章规范**：`constitution/`（顶层 13 章 + 7 部子规范）
- **性能对比**：[docs/hetu-hammurabi/engine-benchmark.md](docs/hetu-hammurabi/engine-benchmark.md)
- **核心理念**：[《宪章编程宣言》](宪章编程宣言.md)

---

项目基于 MIT 协议开源，商用无限制。问题与建议请联系：hetu_altas@163.com
